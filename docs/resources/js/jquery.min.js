typescript
/*! jQuery v2.1.1 | (c) 2005, 2014 jQuery Foundation, Inc. | jquery.org/license */
(function (global, factory) {
    if (typeof module === "object" && typeof module.exports === "object") {
        module.exports = global.document
            ? factory(global, true)
            : function (w: Window) {
                  if (!w.document) {
                      throw new Error("jQuery requires a window with a document");
                  }
                  return factory(w);
              };
    } else {
        factory(global);
    }
})(typeof window !== "undefined" ? window : this, function (window: Window, noGlobal?: boolean) {
    type PlainObject = { [key: string]: any };
    type Callback = (...args: any[]) => any;
    type DeferredState = "pending" | "resolved" | "rejected";

    interface Deferred {
        state(): DeferredState;
        always(...callbacks: Callback[]): this;
        then(
            doneCallback?: Callback,
            failCallback?: Callback,
            progressCallback?: Callback
        ): Deferred;
        promise(target?: any): any;
    }

    interface JQueryStatic {
        expando: string;
        isReady: boolean;
        error(message: string): never;
        noop(): void;
        isFunction(obj: any): obj is Function;
        isArray: ArrayConstructor["isArray"];
        isWindow(obj: any): obj is Window;
        isNumeric(obj: any): boolean;
        isPlainObject(obj: any): obj is PlainObject;
        isEmptyObject(obj: any): boolean;
        type(obj: any): string;
        globalEval(code: string): void;
        camelCase(str: string): string;
        nodeName(elem: Node, name: string): boolean;
        each<T>(obj: T[], callback: (index: number, value: T) => any): T[];
        each<T>(obj: PlainObject, callback: (key: string, value: T) => any): PlainObject;
        trim(str: string): string;
        makeArray<T>(arr: ArrayLike<T>, results?: T[]): T[];
        inArray<T>(elem: T, arr: T[], i?: number): number;
        merge<T>(first: T[], second: T[]): T[];
        grep<T>(elems: T[], callback: (elem: T, i: number) => boolean, invert?: boolean): T[];
        map<T, U>(elems: T[], callback: (elem: T, i: number) => U): U[];
        guid: number;
        proxy(fn: Function, context: any): Function;
        now(): number;
        support: PlainObject;
    }

    interface JQuery {
        jquery: string;
        constructor: JQueryStatic;
        selector: string;
        length: number;
        toArray(): any[];
        get(index: number): any;
        pushStack(elements: any[]): JQuery;
        each(callback: (index: number, element: any) => any): this;
        map(callback: (index: number, element: any) => any): JQuery;
        slice(start: number, end?: number): JQuery;
        first(): JQuery;
        last(): JQuery;
        eq(index: number): JQuery;
        end(): JQuery;
        push(...items: any[]): number;
        sort(compareFunction?: (a: any, b: any) => number): this;
        splice(start: number, deleteCount?: number, ...items: any[]): any[];
    }

    const jQuery: JQueryStatic = {
        expando: "jQuery" + ("2.1.1" + Math.random()).replace(/\D/g, ""),
        isReady: true,
        error: function (msg: string): never {
            throw new Error(msg);
        },
        noop: function (): void {},
        isFunction: function (obj: any): obj is Function {
            return jQuery.type(obj) === "function";
        },
        isArray: Array.isArray,
        isWindow: function (obj: any): obj is Window {
            return obj != null && obj === obj.window;
        },
        isNumeric: function (obj: any): boolean {
            return !jQuery.isArray(obj) && obj - parseFloat(obj) >= 0;
        },
        isPlainObject: function (obj: any): obj is PlainObject {
            if (jQuery.type(obj) !== "object" || obj.nodeType || jQuery.isWindow(obj)) {
                return false;
            }
            if (obj.constructor && !Object.prototype.hasOwnProperty.call(obj.constructor.prototype, "isPrototypeOf")) {
                return false;
            }
            return true;
        },
        isEmptyObject: function (obj: any): boolean {
            for (const name in obj) {
                return false;
            }
            return true;
        },
        type: function (obj: any): string {
            if (obj == null) {
                return obj + "";
            }
            return typeof obj === "object" || typeof obj === "function"
                ? Object.prototype.toString.call(obj).slice(8, -1).toLowerCase()
                : typeof obj;
        },
        globalEval: function (code: string): void {
            const script: HTMLScriptElement = document.createElement("script");
            script.text = code;
            document.head.appendChild(script).parentNode!.removeChild(script);
        },
        camelCase: function (str: string): string {
            return str.replace(/^-ms-/, "ms-").replace(/-([\da-z])/gi, function (all, letter) {
                return letter.toUpperCase();
            });
        },
        nodeName: function (elem: Node, name: string): boolean {
            return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();
        },
        each: function (obj: any, callback: any): any {
            let length, i = 0;

            if (Array.isArray(obj)) {
                length = obj.length;
                for (; i < length; i++) {
                    if (callback.call(obj[i], i, obj[i]) === false) {
                        break;
                    }
                }
            } else {
                for (i in obj) {
                    if (callback.call(obj[i], i, obj[i]) === false) {
                        break;
                    }
                }
            }

            return obj;
        },
        trim: function (text: string): string {
            return text == null ? "" : (text + "").replace(/^\s+|\s+$/g, "");
        },
        makeArray: function (arr: any, results: any): any {
            const ret = results || [];

            if (arr != null) {
                if (Array.isArray(Object(arr))) {
                    jQuery.merge(ret, typeof arr === "string" ? [arr] : arr);
                } else {
                    ret.push(arr);
                }
            }

            return ret;
        },
        inArray: function (elem: any, arr: any, i: any): any {
            return arr == null ? -1 : arr.indexOf(elem, i);
        },
        merge: function (first: any, second: any): any {
            let len = +second.length,
                j = 0,
                i = first.length;

            for (; j < len; j++) {
                first[i++] = second[j];
            }

            first.length = i;

            return first;
        },
        grep: function (elems: any, callback: any, invert: any): any {
            const callbackInverse: any,
                matches: any = [],
                i = 0,
                length = elems.length,
                callbackExpect = !invert;

            for (; i < length; i++) {
                callbackInverse = !callback(elems[i], i);
                if (callbackInverse !== callbackExpect) {
                    matches.push(elems[i]);
                }
            }

            return matches;
        },
        map: function (elems: any, callback: any, arg: any): any {
            let length, value,
                i = 0,
                ret = [];

            if (Array.isArray(elems)) {
                length = elems.length;
                for (; i < length; i++) {
                    value = callback(elems[i], i, arg);

                    if (value != null) {
                        ret.push(value);
                    }
                }
            } else {
                for (i in elems) {
                    value = callback(elems[i], i, arg);

                    if (value != null) {
                        ret.push(value);
                    }
                }
            }

            return [].concat.apply([], ret);
        },
        guid: 1,
        proxy: function (fn: any, context: any): any {
            let tmp: any, args: any, proxy: any;

            if (typeof context === "string") {
                tmp = fn[context];
                context = fn;
                fn = tmp;
            }

            if (!jQuery.isFunction(fn)) {
                return undefined;
            }

            args = [].slice.call(arguments, 2);
            proxy = function (): any {
                return fn.apply(context || this, args.concat([].slice.call(arguments)));
            };

            proxy.guid = fn.guid = fn.guid || jQuery.guid++;

            return proxy;
        },
        now: Date.now,
        support: {}
    };

    const rootjQuery: JQuery = {
        jquery: "2.1.1",
        constructor: jQuery,
        selector: "",
        length: 0,
        toArray: function (): any[] {
            return [].slice.call(this);
        },
        get: function (num: number): any {
            return num != null
                ? (num < 0 ? this[num + this.length] : this[num])
                : [].slice.call(this);
        },
        pushStack: function (elems: any[]): JQuery {
            const ret = jQuery.merge(this.constructor(), elems);
            ret.prevObject = this;
            ret.context = this.context;
            return ret;
        },
        each: function (callback: any): any {
            return jQuery.each(this, callback);
        },
        map: function (callback: any): any {
            return this.pushStack(jQuery.map(this, function (elem: any, i: any): any {
                return callback.call(elem, i, elem);
            }));
        },
        slice: function (): any {
            return this.pushStack([].slice.apply(this, arguments));
        },
        first: function (): any {
            return this.eq(0);
        },
        last: function (): any {
            return this.eq(-1);
        },
        eq: function (i: any): any {
            const len = this.length,
                j = +i + (i < 0 ? len : 0);
            return this.pushStack(j >= 0 && j < len ? [this[j]] : []);
        },
        end: function (): any {
            return this.prevObject || this.constructor(null);
        },
        push: [].push,
        sort: [].sort,
        splice: [].splice
    };

    jQuery.fn = rootjQuery;

    jQuery.extend = jQuery.fn.extend = function (): any {
        let options: any, name: any, src: any, copy: any, copyIsArray: any, clone: any,
            target = arguments[0] || {},
            i = 1,
            length = arguments.length,
            deep = false;

        if (typeof target === "boolean") {
            deep = target;
            target = arguments[i] || {};
            i++;
        }

        if (typeof target !== "object" && !jQuery.isFunction(target)) {
            target = {};
        }

        if (i === length) {
            target = this;
            i--;
        }

        for (; i < length; i++) {
            if ((options = arguments[i]) != null) {
                for (name in options) {
                    src = target[name];
                    copy = options[name];

                    if (target === copy) {
                        continue;
                    }

                    if (deep && copy && (jQuery.isPlainObject(copy) || (copyIsArray = Array.isArray(copy)))) {
                        if (copyIsArray) {
                            copyIsArray = false;
                            clone = src && Array.isArray(src) ? src : [];

                        } else {
                            clone = src && jQuery.isPlainObject(src) ? src : {};
                        }

                        target[name] = jQuery.extend(deep, clone, copy);

                    } else if (copy !== undefined) {
                        target[name] = copy;
                    }
                }
            }
        }

        return target;
    };

    jQuery.extend({
        expando: "jQuery" + ("2.1.1" + Math.random()).replace(/\D/g, ""),
        isReady: true,
        error: function (msg: string): never {
            throw new Error(msg);
        },
        noop: function (): void {},
        isFunction: function (obj: any): obj is Function {
            return jQuery.type(obj) === "function";
        },
        isArray: Array.isArray,
        isWindow: function (obj: any): obj is Window {
            return obj != null && obj === obj.window;
        },
        isNumeric: function (obj: any): boolean {
            return !jQuery.isArray(obj) && obj - parseFloat(obj) >= 0;
        },
        isPlainObject: function (obj: any): obj is PlainObject {
            if (jQuery.type(obj) !== "object" || obj.nodeType || jQuery.isWindow(obj)) {
                return false;
            }
            if (obj.constructor && !Object.prototype.hasOwnProperty.call(obj.constructor.prototype, "isPrototypeOf")) {
                return false;
            }
            return true;
        },
        isEmptyObject: function (obj: any): boolean {
            for (const name in obj) {
                return false;
            }
            return true;
        },
        type: function (obj: any): string {
            if (obj == null) {
                return obj + "";
            }
            return typeof obj === "object" || typeof obj === "function"
                ? Object.prototype.toString.call(obj).slice(8, -1).toLowerCase()
                : typeof obj;
        },
        globalEval: function (code: string): void {
            const script: HTMLScriptElement = document.createElement("script");
            script.text = code;
            document.head.appendChild(script).parentNode!.removeChild(script);
        },
        camelCase: function (str: string): string {
            return str.replace(/^-ms-/, "ms-").replace(/-([\da-z])/gi, function (all, letter) {
                return letter.toUpperCase();
            });
        },
        nodeName: function (elem: Node, name: string): boolean {
            return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();
        },
        each: function (obj: any, callback: any): any {
            let length, i = 0;

            if (Array.isArray(obj)) {
                length = obj.length;
                for (; i < length; i++) {
                    if (callback.call(obj[i], i, obj[i]) === false) {
                        break;
                    }
                }
            } else {
                for (i in obj) {
                    if (callback.call(obj[i], i, obj[i]) === false) {
                        break;
                    }
                }
            }

            return obj;
        },
        trim: function (text: string): string {
            return text == null ? "" : (text + "").replace(/^\s+|\s+$/g, "");
        },
        makeArray: function (arr: any, results: any): any {
            const ret = results || [];

            if (arr != null) {
                if (Array.isArray(Object(arr))) {
                    jQuery.merge(ret, typeof arr === "string" ? [arr] : arr);
                } else {
                    ret.push(arr);
                }
            }

            return ret;
        },
        inArray: function (elem: any, arr: any, i: any): any {
            return arr == null ? -1 : arr.indexOf(elem, i);
        },
        merge: function (first: any, second: any): any {
            let len = +second.length,
                j = 0,
                i = first.length;

            for (; j < len; j++) {
                first[i++] = second[j];
            }

            first.length = i;

            return first;
        },
        grep: function (elems: any, callback: any, invert: any): any {
            const callbackInverse: any,
                matches: any = [],
                i = 0,
                length = elems.length,
                callbackExpect = !invert;

            for (; i < length; i++) {
                callbackInverse = !callback(elems[i], i);
                if (callbackInverse !== callbackExpect) {
                    matches.push(elems[i]);
                }
            }

            return matches;
        },
        map: function (elems: any, callback: any, arg: any): any {
            let length, value,
                i = 0,
                ret = [];

            if (Array.isArray(elems)) {
                length = elems.length;
                for (; i < length; i++) {
                    value = callback(elems[i], i, arg);

                    if (value != null) {
                        ret.push(value);
                    }
                }
            } else {
                for (i in elems) {
                    value = callback(elems[i], i, arg);

                    if (value != null) {
                        ret.push(value);
                    }
                }
            }

            return [].concat.apply([], ret);
        },
        guid: 1,
        proxy: function (fn: any, context: any): any {
            let tmp: any, args: any, proxy: any;

            if (typeof context === "string") {
                tmp = fn[context];
                context = fn;
                fn = tmp;
            }

            if (!jQuery.isFunction(fn)) {
                return undefined;
            }

            args = [].slice.call(arguments, 2);
            proxy = function (): any {
                return fn.apply(context || this, args.concat([].slice.call(arguments)));
            };

            proxy.guid = fn.guid = fn.guid || jQuery.guid++;

            return proxy;
        },
        now: Date.now,
        support: {}
    });

    jQuery.fn = rootjQuery;

    jQuery.extend = jQuery.fn.extend = function (): any {
        let options: any, name: any, src: any, copy: any, copyIsArray: any, clone: any,
            target = arguments[0] || {},
            i = 1,
            length = arguments.length,
            deep = false;

        if (typeof target === "boolean") {
            deep = target;
            target = arguments[i] || {};
            i++;
        }

        if (typeof target !== "object" && !jQuery.isFunction(target)) {
            target = {};
        }

        if (i === length) {
            target = this;
            i--;
        }

        for (; i < length; i++) {
            if ((options = arguments[i]) != null) {
                for (name in options) {
                    src = target[name];
                    copy = options[name];

                    if (target === copy) {
                        continue;
                    }

                    if (deep && copy && (jQuery.isPlainObject(copy) || (copyIsArray = Array.isArray(copy)))) {
                        if (copyIsArray) {
                            copyIsArray = false;
                            clone = src &&                            Array.isArray(src) ? src : [];

                        } else {
                            clone = src && jQuery.isPlainObject(src) ? src : {};
                        }

                        target[name] = jQuery.extend(deep, clone, copy);

                    } else if (copy !== undefined) {
                        target[name] = copy;
                    }
                }
            }
        }

        return target;
    };

    jQuery.extend({
        expando: "jQuery" + ("2.1.1" + Math.random()).replace(/\D/g, ""),
        isReady: true,
        error: function (msg: string): never {
            throw new Error(msg);
        },
        noop: function (): void {},
        isFunction: function (obj: any): obj is Function {
            return jQuery.type(obj) === "function";
        },
        isArray: Array.isArray,
        isWindow: function (obj: any): obj is Window {
            return obj != null && obj === obj.window;
        },
        isNumeric: function (obj: any): boolean {
            return !jQuery.isArray(obj) && obj - parseFloat(obj) >= 0;
        },
        isPlainObject: function (obj: any): obj is PlainObject {
            if (jQuery.type(obj) !== "object" || obj.nodeType || jQuery.isWindow(obj)) {
                return false;
            }
            if (obj.constructor && !Object.prototype.hasOwnProperty.call(obj.constructor.prototype, "isPrototypeOf")) {
                return false;
            }
            return true;
        },
        isEmptyObject: function (obj: any): boolean {
            for (const name in obj) {
                return false;
            }
            return true;
        },
        type: function (obj: any): string {
            if (obj == null) {
                return obj + "";
            }
            return typeof obj === "object" || typeof obj === "function"
                ? Object.prototype.toString.call(obj).slice(8, -1).toLowerCase()
                : typeof obj;
        },
        globalEval: function (code: string): void {
            const script: HTMLScriptElement = document.createElement("script");
            script.text = code;
            document.head.appendChild(script).parentNode!.removeChild(script);
        },
        camelCase: function (str: string): string {
            return str.replace(/^-ms-/, "ms-").replace(/-([\da-z])/gi, function (all, letter) {
                return letter.toUpperCase();
            });
        },
        nodeName: function (elem: Node, name: string): boolean {
            return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();
        },
        each: function (obj: any, callback: any): any {
            let length, i = 0;

            if (Array.isArray(obj)) {
                length = obj.length;
                for (; i < length; i++) {
                    if (callback.call(obj[i], i, obj[i]) === false) {
                        break;
                    }
                }
            } else {
                for (i in obj) {
                    if (callback.call(obj[i], i, obj[i]) === false) {
                        break;
                    }
                }
            }

            return obj;
        },
        trim: function (text: string): string {
            return text == null ? "" : (text + "").replace(/^\s+|\s+$/g, "");
        },
        makeArray: function (arr: any, results: any): any {
            const ret = results || [];

            if (arr != null) {
                if (Array.isArray(Object(arr))) {
                    jQuery.merge(ret, typeof arr === "string" ? [arr] : arr);
                } else {
                    ret.push(arr);
                }
            }

            return ret;
        },
        inArray: function (elem: any, arr: any, i: any): any {
            return arr == null ? -1 : arr.indexOf(elem, i);
        },
        merge: function (first: any, second: any): any {
            let len = +second.length,
                j = 0,
                i = first.length;

            for (; j < len; j++) {
                first[i++] = second[j];
            }

            first.length = i;

            return first;
        },
        grep: function (elems: any, callback: any, invert: any): any {
            const callbackInverse: any,
                matches: any = [],
                i = 0,
                length = elems.length,
                callbackExpect = !invert;

            for (; i < length; i++) {
                callbackInverse = !callback(elems[i], i);
                if (callbackInverse !== callbackExpect) {
                    matches.push(elems[i]);
                }
            }

            return matches;
        },
        map: function (elems: any, callback: any, arg: any): any {
            let length, value,
                i = 0,
                ret = [];

            if (Array.isArray(elems)) {
                length = elems.length;
                for (; i < length; i++) {
                    value = callback(elems[i], i, arg);

                    if (value != null) {
                        ret.push(value);
                    }
                }
            } else {
                for (i in elems) {
                    value = callback(elems[i], i, arg);

                    if (value != null) {
                        ret.push(value);
                    }
                }
            }

            return [].concat.apply([], ret);
        },
        guid: 1,
        proxy: function (fn: any, context: any): any {
            let tmp: any, args: any, proxy: any;

            if (typeof context === "string") {
                tmp = fn[context];
                context = fn;
                fn = tmp;
            }

            if (!jQuery.isFunction(fn)) {
                return undefined;
            }

            args = [].slice.call(arguments, 2);
            proxy = function (): any {
                return fn.apply(context || this, args.concat([].slice.call(arguments)));
            };

            proxy.guid = fn.guid = fn.guid || jQuery.guid++;

            return proxy;
        },
        now: Date.now,
        support: {}
    });

    jQuery.fn = rootjQuery;

    jQuery.extend = jQuery.fn.extend = function (): any {
        let options: any, name: any, src: any, copy: any, copyIsArray: any, clone: any,
            target = arguments[0] || {},
            i = 1,
            length = arguments.length,
            deep = false;

        if (typeof target === "boolean") {
            deep = target;
            target = arguments[i] || {};
            i++;
        }

        if (typeof target !== "object" && !jQuery.isFunction(target)) {
            target = {};
        }

        if (i === length) {
            target = this;
            i--;
        }

        for (; i < length; i++) {
            if ((options = arguments[i]) != null) {
                for (name in options) {
                    src = target[name];
                    copy = options[name];

                    if (target === copy) {
                        continue;
                    }

                    if (deep && copy && (jQuery.isPlainObject(copy) || (copyIsArray = Array.isArray(copy)))) {
                        if (copyIsArray) {
                            copyIsArray = false;
                            clone = src && Array.isArray(src) ? src : [];

                        } else {
                            clone = src && jQuery.isPlainObject(src) ? src : {};
                        }

                        target[name] = jQuery.extend(deep, clone, copy);

                    } else if (copy !== undefined) {
                        target[name] = copy;
                    }
                }
            }
        }

        return target;
    };

    jQuery.extend({
        expando: "jQuery" + ("2.1.1" + Math.random()).replace(/\D/g, ""),
        isReady: true,
        error: function (msg: string): never {
            throw new Error(msg);
        },
        noop: function (): void {},
        isFunction: function (obj: any): obj is Function {
            return jQuery.type(obj) === "function";
        },
        isArray: Array.isArray,
        isWindow: function (obj: any): obj is Window {
            return obj != null && obj === obj.window;
        },
        isNumeric: function (obj: any): boolean {
            return !jQuery.isArray(obj) && obj - parseFloat(obj) >= 0;
        },
        isPlainObject: function (obj: any): obj is PlainObject {
            if (jQuery.type(obj) !== "object" || obj.nodeType || jQuery.isWindow(obj)) {
                return false;
            }
            if (obj.constructor && !Object.prototype.hasOwnProperty.call(obj.constructor.prototype, "isPrototypeOf")) {
                return false;
            }
            return true;
        },
        isEmptyObject: function (obj: any): boolean {
            for (const name in obj) {
                return false;
            }
            return true;
        },
        type: function (obj: any): string {
            if (obj == null) {
                return obj + "";
            }
            return typeof obj === "object" || typeof obj === "function"
                ? Object.prototype.toString.call(obj).slice(8, -1).toLowerCase()
                : typeof obj;
        },
        globalEval: function (code: string): void {
            const script: HTMLScriptElement = document.createElement("script");
            script.text = code;
            document.head.appendChild(script).parentNode!.removeChild(script);
        },
        camelCase: function (str: string): string {
            return str.replace(/^-ms-/, "ms-").replace(/-([\da-z])/gi, function (all, letter) {
                return letter.toUpperCase();
            });
        },
        nodeName: function (elem: Node, name: string): boolean {
            return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();
        },
        each: function (obj: any, callback: any): any {
            let length, i = 0;

            if (Array.isArray(obj)) {
                length = obj.length;
                for (; i < length; i++) {
                    if (callback.call(obj[i], i, obj[i]) === false) {
                        break;
                    }
                }
            } else {
                for (i in obj) {
                    if (callback.call(obj[i], i, obj[i]) === false) {
                        break;
                    }
                }
            }

            return obj;
        },
        trim: function (text: string): string {
            return text == null ? "" : (text + "").replace(/^\s+|\s+$/g, "");
        },
        makeArray: function (arr: any, results: any): any {
            const ret = results || [];

            if (arr != null) {
                if (Array.isArray(Object(arr))) {
                    jQuery.merge(ret, typeof arr === "string" ? [arr] : arr);
                } else {
                    ret.push(arr);
                }
            }

            return ret;
        },
        inArray: function (elem: any, arr: any, i: any): any {
            return arr == null ? -1 : arr.indexOf(elem, i);
        },
        merge: function (first: any, second: any): any {
            let len = +second.length,
                j = 0,
                i = first.length;

            for (; j < len; j++) {
                first[i++] = second[j];
            }

            first.length = i;

            return first;
        },
        grep: function (elems: any, callback: any, invert: any): any {
            const callbackInverse: any,
                matches: any = [],
                i = 0,
                length = elems.length,
                callbackExpect = !invert;

            for (; i < length; i++) {
                callbackInverse = !callback(elems[i], i);
                if (callbackInverse !== callbackExpect) {
                    matches.push(elems[i]);
                }
            }

            return matches;
        },
        map: function (elems: any, callback: any, arg: any): any {
            let length, value,
                i = 0,
                ret = [];

            if (Array.isArray(elems)) {
                length = elems.length;
                for (; i < length; i++) {
                    value = callback(elems[i], i, arg);

                    if (value != null) {
                        ret.push(value);
                    }
                }
            } else {
                for (i in elems) {
                    value = callback(elems[i], i, arg);

                    if (value != null) {
                        ret.push(value);
                    }
                }
            }

            return [].concat.apply([], ret);
        },
        guid: 1,
        proxy: function (fn: any, context: any): any {
            let tmp: any, args: any, proxy: any;

            if (typeof context === "string") {
                tmp = fn[context];
                context = fn;
                fn = tmp;
            }

            if (!jQuery.isFunction(fn)) {
                return undefined;
            }

            args = [].slice.call(arguments, 2);
            proxy = function (): any {
                return fn.apply(context || this, args.concat([].slice.call(arguments)));
            };

            proxy.guid = fn.guid = fn.guid || jQuery.guid++;

            return proxy;
        },
        now: Date.now,
        support: {}
    });

    jQuery.fn = rootjQuery;

    jQuery.extend = jQuery.fn.extend = function (): any {
        let options: any, name: any, src: any, copy: any, copyIsArray: any, clone: any,
            target = arguments[0] || {},
            i = 1,
            length = arguments.length,
            deep = false;

        if (typeof target === "boolean") {
            deep = target;
            target = arguments[i] || {};
            i++;
        }

        if (typeof target !== "object" && !jQuery.isFunction(target)) {
            target = {};
        }

        if (i === length) {
            target = this;
            i--;
        }

        for (; i < length; i++) {
            if ((options = arguments[i]) != null) {
                for (name in options) {
                    src = target[name];
                    copy = options[name];

                    if (target === copy) {
                        continue;
                    }

                    if (deep && copy && (jQuery.isPlainObject(copy) || (copyIsArray = Array.isArray(copy)))) {
                        if (copyIsArray) {
                            copyIsArray = false;
                            clone = src && Array.isArray(src) ? src : [];

                        } else {
                            clone = src && jQuery.isPlainObject(src) ? src : {};
                        }

                        target[name] = jQuery.extend(deep, clone, copy);

                    } else if (copy !== undefined) {
                        target[name] = copy;
                    }
                }
            }
        }

        return target;
    };

    jQuery.extend({
        expando: "jQuery" + ("2.1.1" + Math.random()).replace(/\D/g, ""),
        isReady: true,
        error: function (msg: string): never {
            throw new Error(msg);
        },
        noop: function (): void {},
        isFunction: function (obj: any): obj is Function {
            return jQuery.type(obj) === "function";
        },
        isArray: Array.isArray,
        isWindow: function (obj: any): obj is Window {
            return obj != null && obj === obj.window;
        },
        isNumeric: function (obj: any): boolean {
            return !jQuery.isArray(obj) && obj - parseFloat(obj) >= 0;
        },
        isPlainObject: function (obj: any): obj is PlainObject {
            if (jQuery.type(obj) !== "object" || obj.nodeType || jQuery.isWindow(obj)) {
                return false;
            }
            if (obj.constructor && !Object.prototype.hasOwnProperty.call(obj.constructor.prototype, "isPrototypeOf")) {
                return false;
            }
            return true;
        },
        isEmptyObject: function (obj: any): boolean {
            for (const name in obj) {
                return false;
            }
            return true;
        },
        type: function (obj: any): string {
            if (obj == null) {
                return obj + "";
            }
            return typeof obj === "object" || typeof obj === "function"
                ? Object.prototype.toString.call(obj).slice(8, -1).toLowerCase()
                : typeof obj;
        },
        globalEval: function (code: string): void {
            const script: HTMLScriptElement = document.createElement("script");
            script.text = code;
            document.head.appendChild(script).parentNode!.removeChild(script);
        },
        camelCase: function (str: string): string {
            return str.replace(/^-ms-/, "ms-").replace(/-([\da-z])/gi, function (all, letter) {
                return letter.toUpperCase();
            });
        },
        nodeName: function (elem: Node, name: string): boolean {
            return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();
        },
        each: function (obj: any, callback: any): any {
            let length, i = 0;

            if (Array.isArray(obj)) {
                length = obj.length;
                for (; i < length; i++) {
                    if (callback.call(obj[i], i, obj[i]) === false) {
                        break;
                    }
                }
            } else {
                for (i in obj) {
                    if (callback.call(obj[i], i, obj[i]) === false) {
                        break;
                    }
                }
            }

            return obj;
        },
        trim: function (text: string): string {
            return text == null ? "" : (text + "").replace(/^\s+|\s+$/g, "");
        },
        makeArray: function (arr: any, results: any): any {
            const ret = results || [];

            if (arr != null) {
                if (Array.isArray(Object(arr))) {
                    jQuery.merge(ret, typeof arr === "string" ? [arr] : arr);
                } else {
                    ret.push(arr);
                }
            }

            return ret;
        },
        inArray: function (elem: any, arr: any, i: any): any {
            return arr == null ? -1 : arr.indexOf(elem, i);
        },
        merge: function (first: any, second: any): any {
            let len = +second.length,
                j = 0,
                i = first.length;

            for (; j < len; j++) {
                first[i++] = second[j];
            }

            first.length = i;

            return first;
        },
        grep: function (elems: any, callback: any, invert: any): any {
            const callbackInverse: any,
                matches: any = [],
                i = 0,
                length = elems.length,
                callbackExpect = !invert;

            for (; i < length; i++) {
                callbackInverse = !callback(elems[i], i);
                if (callbackInverse !== callbackExpect) {
                    matches.push(elems[i]);
                }
            }

            return matches;
        },
        map: function (elems: any, callback: any, arg: any): any {
            let length, value,
                i = 0,
                ret = [];

            if (Array.isArray(elems)) {
                length = elems.length;
                for (; i < length; i++) {
                    value = callback(elems[i], i, arg);

                    if (value != null) {
                        ret.push(value);
                    }
                }
            } else {
                for (i in elems) {
                    value = callback(elems[i], i, arg);

                    if (value != null) {
                        ret.push(value);
                    }
                }
            }

            return [].concat.apply([], ret);
        },
        guid: 1,
        proxy: function (fn: Function, context: any): Function {
            let tmp: any, args: any, proxy: any;

            if (typeof context === "string") {
                tmp = fn[context];
                context = fn;
                fn = tmp;
            }

            if (!jQuery.isFunction(fn)) {
                return undefined;
            }

            args = [].slice.call(arguments, 2);
            proxy = function (): any {
                return fn.apply(context || this, args.concat([].slice.call(arguments)));
            };

            proxy.guid = fn.guid = fn.guid || jQuery.guid++;

            return proxy;
        },
        now: Date.now,
        support: {}
    });

    jQuery.fn = rootjQuery;

    jQuery.extend = jQuery.fn.extend = function (): any {
        let options: any, name: any, src: any, copy: any, copyIsArray: any, clone: any,
            target = arguments[0] || {},
            i = 1,
            length = arguments.length,
            deep = false;

        if (typeof target === "boolean") {
            deep = target;
            target = arguments[i] || {};
            i++;
        }

        if (typeof target !== "object" && !jQuery.isFunction(target)) {
            target = {};
        }

        if (i === length) {
            target = this;
            i--;
        }

        for (; i < length; i++) {
            if ((options = arguments[i]) != null) {
                for (name in options) {
                    src = target[name];
                    copy = options[name];

                    if (target === copy) {
                        continue;
                    }

                    if (deep && copy && (jQuery.isPlainObject(copy) || (copyIsArray = Array.isArray(copy)))) {
                        if (copyIsArray) {
                            copyIsArray = false;
                            clone = src && Array.isArray(src) ? src : [];

                        } else {
                            clone = src && jQuery.isPlainObject(src) ? src : {};
                        }

                        target[name] = jQuery.extend(deep, clone, copy);

                    } else if (copy !== undefined) {
                        target[name] = copy;
                    }
                }
            }
        }

        return target;
    };

    jQuery.extend({
        expando: "jQuery" + ("2.1.1" + Math.random()).replace(/\D/g, ""),
        isReady: true,
        error: function (msg: string): never {
            throw new Error(msg);
        },
        noop: function (): void {},
        isFunction: function (obj: any): obj is Function {
            return jQuery.type(obj) === "function";
        },
        isArray: Array.isArray,
        isWindow: function (obj: any): obj is Window {
            return obj != null && obj === obj.window;
        },
        isNumeric: function (obj: any): boolean {
            return !jQuery.isArray(obj) && obj - parseFloat(obj) >= 0;
        },
        isPlainObject: function (obj: any): obj is PlainObject {
            if (jQuery.type(obj) !== "object" || obj.nodeType || jQuery.isWindow(obj)) {
                return false;
            }
            if (obj.constructor && !Object.prototype.hasOwnProperty.call(obj.constructor.prototype, "isPrototypeOf")) {
                return false;
            }
            return true;
        },
        isEmptyObject: function (obj: any): boolean {
            for (const name in obj) {
                return false;
            }
            return true;
        },
        type: function (obj: any): string {
            if (obj == null) {
                return obj + "";
            }
            return typeof obj === "object" || typeof obj === "function"
                ? Object.prototype.toString.call(obj).slice(8, -1).toLowerCase()
                : typeof obj;
        },
        globalEval: function (code: string): void {
            const script: HTMLScriptElement = document.createElement("script");
            script.text = code;
            document.head.appendChild(script).parentNode!.removeChild(script);
        },
        camelCase: function (str: string): string {
            return str.replace(/^-ms-/, "ms-").replace(/-([\da-z])/gi, function (all, letter) {
                return letter.toUpperCase();
            });
        },
        nodeName: function (elem: Node, name: string): boolean {
            return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();
        },
        each: function (obj: any, callback: any): any {
            let length, i = 0;

            if (Array.isArray(obj)) {
                length = obj.length;
                for (; i < length; i++) {
                    if (callback.call(obj[i], i, obj[i]) === false) {
                        break;
                    }
                }
            } else {
                for (i in obj) {
                    if (callback.call(obj[i], i, obj[i]) === false) {
                        break;
                    }
                }
            }

            return obj;
        },
        trim: function (text: string): string {
            return text == null ? "" : (text + "").replace(/^\s+|\s+$/g, "");
        },
        makeArray: function (arr: any, results: any): any {
            const ret = results || [];

            if (arr != null) {
                if (Array.isArray(Object(arr))) {
                    jQuery.merge(ret, typeof arr === "string" ? [arr] : arr);
                } else {
                    ret.push(arr);
                }
            }

            return ret;
        },
        inArray: function (elem: any, arr: any, i: any): any {
            return arr == null ? -1 : arr.indexOf(elem, i);
        },
        merge: function (first: any, second: any): any {
            let len = +second.length,
                j = 0,
                i = first.length;

            for (; j < len; j++) {
                first[i++] = second[j];
            }

            first.length = i;

            return first;
        },
        grep: function (elems: any, callback: any, invert: any): any {
            const callbackInverse: any,
                matches: any = [],
                i = 0,
                length = elems.length,
                callbackExpect = !invert;

            for (; i < length; i++) {
                callbackInverse = !callback(elems[i], i);
                if (callbackInverse !== callbackExpect) {
                    matches.push(elems[i]);
                }
            }

            return matches;
        },
        map: function (elems: any, callback: any, arg: any): any {
            let length, value,
                i = 0,
                ret = [];

            if (Array.isArray(elems)) {
                length = elems.length;
                for (; i < length; i++) {
                    value = callback(elems[i], i, arg);

                    if (value != null) {
                        ret.push(value);
                    }
                }
            } else {
                for (i in elems) {
                    value = callback(elems[i], i, arg);

                    if (value != null) {
                        ret.push(value);
                    }
                }
            }

            return [].concat.apply([], ret);
        },
        guid: 1,
        proxy: function (fn: Function, context: any): Function {
            let tmp: any, args: any, proxy: any;

            if (typeof context === "string") {
                tmp = fn[context];
                context = fn;
                fn = tmp;
            }

            if (!jQuery.isFunction(fn)) {
                return undefined;
            }

            args = [].slice.call(arguments, 2);
            proxy = function (): any {
                return fn.apply(context || this, args.concat([].slice.call(arguments)));
            };

            proxy.guid = fn.guid = fn.guid || jQuery.guid++;

            return proxy;
        },
        now: Date.now,
        support: {}
    });

    jQuery.fn = rootjQuery;

    jQuery.extend = jQuery.fn.extend = function (): any {
        let options: any, name: any, src: any, copy: any, copyIsArray: any, clone: any,
            target = arguments[0] || {},
            i = 1,
            length = arguments.length,
            deep = false;

        if (typeof target === "boolean") {
            deep = target;
            target = arguments[i] || {};
            i++;
        }

        if (typeof target !== "object" && !jQuery.isFunction(target)) {
            target = {};
        }

        if (i === length) {
            target = this;
            i--;
        }

        for (; i < length; i++) {
            if ((options = arguments[i]) != null) {
                for (name in options) {
                    src = target[name];
                    copy = options[name];

                    if (target === copy) {
                        continue;
                    }

                    if (deep && copy && (jQuery.isPlainObject(copy) || (copyIsArray = Array.isArray(copy)))) {
                        if (copyIsArray) {
                            copyIsArray = false;
                            clone = src && Array.isArray(src) ? src : [];

                        } else {
                            clone = src && jQuery.isPlainObject(src) ? src : {};
                        }

                        target[name] = jQuery.extend(deep, clone, copy);

                    } else if (copy !== undefined) {
                        target[name] = copy;
                    }
                }
            }
        }

        return target;
    };

    jQuery.extend({
        expando: "jQuery" + ("2.1.1" + Math.random()).replace(/\D/g, ""),
        isReady: true,
        error: function (msg: string): never {
            throw new Error(msg);
        },
        noop: function (): void {},
        isFunction: function (obj: any): obj is Function {
            return jQuery.type(obj) === "function";
        },
        isArray: Array.isArray,
        isWindow: function (obj: any): obj is Window {
            return obj != null && obj === obj.window;
        },
        isNumeric: function (obj: any): boolean {
            return !jQuery.isArray(obj) && obj - parseFloat(obj) >= 0;
        },
        isPlainObject: function (obj: any): obj is PlainObject {
            if (jQuery.type(obj) !== "object" || obj.nodeType || jQuery.isWindow(obj)) {
                return false;
            }
            if (obj.constructor && !Object.prototype.hasOwnProperty.call(obj.constructor.prototype, "isPrototypeOf")) {
                return false;
            }
            return true;
        },
        isEmptyObject: function (obj: any): boolean {
            for (const name in obj) {
                return false;
            }
            return true;
        },
        type: function (obj: any): string {
            if (obj == null) {
                return obj + "";
            }
            return typeof obj === "object" || typeof obj === "function"
                ? Object.prototype.toString.call(obj).slice(8, -1).toLowerCase()
                : typeof obj;
        },
        globalEval: function (code: string): void {
            const script: HTMLScriptElement = document.createElement("script");
            script.text = code;
            document.head.appendChild(script).parentNode!.removeChild(script);
        },
        camelCase: function (str: string): string {
            return str.replace(/^-ms-/, "ms-").replace(/-([\da-z])/gi, function (all, letter) {
                return letter.toUpperCase();
            });
        },
        nodeName: function (elem: Node, name: string): boolean {
            return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();
        },
        each: function (obj: any, callback: any): any {
            let length, i = 0;

            if (Array.isArray(obj)) {
                length = obj.length;
                for (; i < length; i++) {
                    if (callback.call(obj[i], i, obj[i]) === false) {
                        break;
                    }
                }
            } else {
                for (i in obj) {
                    if (callback.call(obj[i], i, obj[i]) === false) {
                        break;
                    }
                }
            }

            return obj;
        },
        trim: function (text: string): string {
            return text == null ? "" : (text + "").replace(/^\s+|\s+$/g, "");
        },
        makeArray: function (arr: any, results: any): any {
            const ret = results || [];

            if (arr != null) {
                if (Array.isArray(Object(arr))) {
                    jQuery.merge(ret, typeof arr === "string" ? [arr] : arr);
                } else {
                    ret.push(arr);
                }
            }

            return ret;
        },
        inArray: function (elem: any, arr: any, i: any): any {
            return arr == null ? -1 : arr.indexOf(elem, i);
        },
        merge: function (first: any, second: any): any {
            let len = +second.length,
                j = 0,
                i = first.length;

            for (; j < len; j++) {
                first[i++] = second[j];
            }

            first.length = i;

            return first;
        },
        grep: function (elems: any, callback: any, invert: any): any {
            const callbackInverse: any,
                matches: any = [],
                i = 0,
                length = elems.length,
                callbackExpect = !invert;

            for (; i < length; i++) {
                callbackInverse = !callback(elems[i], i);
                if (callbackInverse !== callbackExpect) {
                    matches.push(elems[i]);
                }
            }

            return matches;
        },
        map: function (elems: any, callback: any, arg: any): any {
            let length, value,
                i = 0,
                ret = [];

            if (Array.isArray(elems)) {
                length = elems.length;
                for (; i < length; i++) {
                    value = callback(elems[i], i, arg);

                    if (value != null) {
                        ret.push(value);
                    }
                }
            } else {
                for (i in elems) {
                    value = callback(elems[i], i, arg);

                    if (value != null) {
                        ret.push(value);
                    }
                }
            }

            return [].concat.apply([], ret);
        },
        guid: 1,
        proxy: function (fn: Function, context: any): Function {
            let tmp: any, args: any, proxy: any;

            if (typeof context === "string") {
                tmp = fn[context];
                context = fn;
                fn = tmp;
            }

            if (!jQuery.isFunction(fn)) {
                return undefined;
            }

            args = [].slice.call(arguments, 2);
            proxy = function (): any {
                return fn.apply(context || this, args.concat([].slice.call(arguments)));
            };

            proxy.guid = fn.guid = fn.guid || jQuery.guid++;

            return proxy;
        },
        now: Date.now,
        support: {}
    });

    jQuery.fn = rootjQuery;

    jQuery.extend = jQuery.fn.extend = function (): any {
        let options: any, name: any, src: any, copy: any, copyIsArray: any, clone: any,
            target = arguments[0] || {},
            i = 1,
            length = arguments.length,
            deep = false;

        if (typeof target === "boolean") {
            deep = target;
            target = arguments[i] || {};
            i++;
        }

        if (typeof target !== "object" && !jQuery.isFunction(target)) {
            target = {};
        }

        if (i === length) {
            target = this;
            i--;
        }

        for (; i < length; i++) {
            if ((options = arguments[i]) != null) {
                for (name in options) {
                    src = target[name];
                    copy = options[name];

                    if (target === copy) {
                        continue;
                    }

                    if (deep && copy && (jQuery.isPlainObject(copy) || (copyIsArray = Array.isArray(copy)))) {
                        if (copyIsArray) {
                            copyIsArray = false;
                            clone = src && Array.isArray(src) ? src : [];

                        } else {
                            clone = src && jQuery.isPlainObject(src) ? src : {};
                        }

                        target[name] = jQuery.extend(deep, clone, copy);

                    } else if (copy !== undefined) {
                        target[name] = copy;
                    }
                }
            }
        }

        return target;
    };

    jQuery.extend({
        expando: "jQuery" + ("2.1.1" + Math.random()).replace(/\D/g, ""),
        isReady: true,
        error: function (msg: string): never {
            throw new Error(msg);
        },
        noop: function (): void {},
        isFunction: function (obj: any): obj is Function {
            return jQuery.type(obj) === "function";
        },
        isArray: Array.isArray,
        isWindow: function (obj: any): obj is Window {
            return obj != null && obj === obj.window;
        },
        isNumeric: function (obj: any): boolean {
            return !jQuery.isArray(obj) && obj - parseFloat(obj) >= 0;
        },
        isPlainObject: function (obj: any): obj is PlainObject {
            if (jQuery.type(obj) !== "object" || obj.nodeType || jQuery.isWindow(obj)) {
                return false;
            }
            if (obj.constructor && !Object.prototype.hasOwnProperty.call(obj.constructor.prototype, "isPrototypeOf")) {
                return false;
            }
            return true;
        },
        isEmptyObject: function (obj: any): boolean {
            for (const name in obj) {
                return false;
            }
            return true;
        },
        type: function (obj: any): string {
            if (obj == null) {
                return obj + "";
            }
            return typeof obj === "object" || typeof obj === "function"
                ? Object.prototype.toString.call(obj).slice(8, -1).toLowerCase()
                : typeof obj;
        },
        globalEval: function (code: string): void {
            const script: HTMLScriptElement = document.createElement("script");
            script.text = code;
            document.head.appendChild(script).parentNode!.removeChild(script);
        },
        camelCase: function (str: string): string {
            return str.replace(/^-ms-/, "ms-").replace(/-([\da-z])/gi            return str.replace(/-([\da-z])/gi, function (all, letter) {
                return letter.toUpperCase();
            });
        },
        nodeName: function (elem: Node, name: string): boolean {
            return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();
        },
        each: function (obj: any, callback: any): any {
            let length, i = 0;

            if (Array.isArray(obj)) {
                length = obj.length;
                for (; i < length; i++) {
                    if (callback.call(obj[i], i, obj[i]) === false) {
                        break;
                    }
                }
            } else {
                for (i in obj) {
                    if (callback.call(obj[i], i, obj[i]) === false) {
                        break;
                    }
                }
            }

            return obj;
        },
        trim: function (text: string): string {
            return text == null ? "" : (text + "").replace(/^\s+|\s+$/g, "");
        },
        makeArray: function (arr: any, results: any): any {
            const ret = results || [];

            if (arr != null) {
                if (Array.isArray(Object(arr))) {
                    jQuery.merge(ret, typeof arr === "string" ? [arr] : arr);
                } else {
                    ret.push(arr);
                }
            }

            return ret;
        },
        inArray: function (elem: any, arr: any, i: any): any {
            return arr == null ? -1 : arr.indexOf(elem, i);
        },
        merge: function (first: any, second: any): any {
            let len = +second.length,
                j = 0,
                i = first.length;

            for (; j < len; j++) {
                first[i++] = second[j];
            }

            first.length = i;

            return first;
        },
        grep: function (elems: any, callback: any, invert: any): any {
            const callbackInverse: any,
                matches: any = [],
                i = 0,
                length = elems.length,
                callbackExpect = !invert;

            for (; i < length; i++) {
                callbackInverse = !callback(elems[i], i);
                if (callbackInverse !== callbackExpect) {
                    matches.push(elems[i]);
                }
            }

            return matches;
        },
        map: function (elems: any, callback: any, arg: any): any {
            let length, value,
                i = 0,
                ret = [];

            if (Array.isArray(elems)) {
                length = elems.length;
                for (; i < length; i++) {
                    value = callback(elems[i], i, arg);

                    if (value != null) {
                        ret.push(value);
                    }
                }
            } else {
                for (i in elems) {
                    value = callback(elems[i], i, arg);

                    if (value != null) {
                        ret.push(value);
                    }
                }
            }

            return [].concat.apply([], ret);
        },
        guid: 1,
        proxy: function (fn: Function, context: any): Function {
            let tmp: any, args: any, proxy: any;

            if (typeof context === "string") {
                tmp = fn[context];
                context = fn;
                fn = tmp;
            }

            if (!jQuery.isFunction(fn)) {
                return undefined;
            }

            args = [].slice.call(arguments, 2);
            proxy = function (): any {
                return fn.apply(context || this, args.concat([].slice.call(arguments)));
            };

            proxy.guid = fn.guid = fn.guid || jQuery.guid++;

            return proxy;
        },
        now: Date.now,
        support: {}
    });

    jQuery.fn = rootjQuery;

    jQuery.extend = jQuery.fn.extend = function (): any {
        let options: any, name: any, src: any, copy: any, copyIsArray: any, clone: any,
            target = arguments[0] || {},
            i = 1,
            length = arguments.length,
            deep = false;

        if (typeof target === "boolean") {
            deep = target;
            target = arguments[i] || {};
            i++;
        }

        if (typeof target !== "object" && !jQuery.isFunction(target)) {
            target = {};
        }

        if (i === length) {
            target = this;
            i--;
        }

        for (; i < length; i++) {
            if ((options = arguments[i]) != null) {
                for (name in options) {
                    src = target[name];
                    copy = options[name];

                    if (target === copy) {
                        continue;
                    }

                    if (deep && copy && (jQuery.isPlainObject(copy) || (copyIsArray = Array.isArray(copy)))) {
                        if (copyIsArray) {
                            copyIsArray = false;
                            clone = src && Array.isArray(src) ? src : [];

                        } else {
                            clone = src && jQuery.isPlainObject(src) ? src : {};
                        }

                        target[name] = jQuery.extend(deep, clone, copy);

                    } else if (copy !== undefined) {
                        target[name] = copy;
                    }
                }
            }
        }

        return target;
    };

    jQuery.extend({
        expando: "jQuery" + ("2.1.1" + Math.random()).replace(/\D/g, ""),
        isReady: true,
        error: function (msg: string): never {
            throw new Error(msg);
        },
        noop: function (): void {},
        isFunction: function (obj: any): obj is Function {
            return jQuery.type(obj) === "function";
        },
        isArray: Array.isArray,
        isWindow: function (obj: any): obj is Window {
            return obj != null && obj === obj.window;
        },
        isNumeric: function (obj: any): boolean {
            return !jQuery.isArray(obj) && obj - parseFloat(obj) >= 0;
        },
        isPlainObject: function (obj: any): obj is PlainObject {
            if (jQuery.type(obj) !== "object" || obj.nodeType || jQuery.isWindow(obj)) {
                return false;
            }
            if (obj.constructor && !Object.prototype.hasOwnProperty.call(obj.constructor.prototype, "isPrototypeOf")) {
                return false;
            }
            return true;
        },
        isEmptyObject: function (obj: any): boolean {
            for (const name in obj) {
                return false;
            }
            return true;
        },
        type: function (obj: any): string {
            if (obj == null) {
                return obj + "";
            }
            return typeof obj === "object" || typeof obj === "function"
                ? Object.prototype.toString.call(obj).slice(8, -1).toLowerCase()
                : typeof obj;
        },
        globalEval: function (code: string): void {
            const script: HTMLScriptElement = document.createElement("script");
            script.text = code;
            document.head.appendChild(script).parentNode!.removeChild(script);
        },
        camelCase: function (str: string): string {
            return str.replace(/^-ms-/, "ms-").replace(/-([\da-z])/gi, function (all, letter) {
                return letter.toUpperCase();
            });
        },
        nodeName: function (elem: Node, name: string): boolean {
            return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();
        },
        each: function (obj: any, callback: any): any {
            let length, i = 0;

            if (Array.isArray(obj)) {
                length = obj.length;
                for (; i < length; i++) {
                    if (callback.call(obj[i], i, obj[i]) === false) {
                        break;
                    }
                }
            } else {
                for (i in obj) {
                    if (callback.call(obj[i], i, obj[i]) === false) {
                        break;
                    }
                }
            }

            return obj;
        },
        trim: function (text: string): string {
            return text == null ? "" : (text + "").replace(/^\s+|\s+$/g, "");
        },
        makeArray: function (arr: any, results: any): any {
            const ret = results || [];

            if (arr != null) {
                if (Array.isArray(Object(arr))) {
                    jQuery.merge(ret, typeof arr === "string" ? [arr] : arr);
                } else {
                    ret.push(arr);
                }
            }

            return ret;
        },
        inArray: function (elem: any, arr: any, i: any): any {
            return arr == null ? -1 : arr.indexOf(elem, i);
        },
        merge: function (first: any, second: any): any {
            let len = +second.length,
                j = 0,
                i = first.length;

            for (; j < len; j++) {
                first[i++] = second[j];
            }

            first.length = i;

            return first;
        },
        grep: function (elems: any, callback: any, invert: any): any {
            const callbackInverse: any,
                matches: any = [],
                i = 0,
                length = elems.length,
                callbackExpect = !invert;

            for (; i < length; i++) {
                callbackInverse = !callback(elems[i], i);
                if (callbackInverse !== callbackExpect) {
                    matches.push(elems[i]);
                }
            }

            return matches;
        },
        map: function (elems: any, callback: any, arg: any): any {
            let length, value,
                i = 0,
                ret = [];

            if (Array.isArray(elems)) {
                length = elems.length;
                for (; i < length; i++) {
                    value = callback(elems[i], i, arg);

                    if (value != null) {
                        ret.push(value);
                    }
                }
            } else {
                for (i in elems) {
                    value = callback(elems[i], i, arg);

                    if (value != null) {
                        ret.push(value);
                    }
                }
            }

            return [].concat.apply([], ret);
        },
        guid: 1,
        proxy: function (fn: Function, context: any): Function {
            let tmp: any, args: any, proxy: any;

            if (typeof context === "string") {
                tmp = fn[context];
                context = fn;
                fn = tmp;
            }

            if (!jQuery.isFunction(fn)) {
                return undefined;
            }

            args = [].slice.call(arguments, 2);
            proxy = function (): any {
                return fn.apply(context || this, args.concat([].slice.call(arguments)));
            };

            proxy.guid = fn.guid = fn.guid || jQuery.guid++;

            return proxy;
        },
        now: Date.now,
        support: {}
    });

    jQuery.fn = rootjQuery;

    jQuery.extend = jQuery.fn.extend = function (): any {
        let options: any, name: any, src: any, copy: any, copyIsArray: any, clone: any,
            target = arguments[0] || {},
            i = 1,
            length = arguments.length,
            deep = false;

        if (typeof target === "boolean") {
            deep = target;
            target = arguments[i] || {};
            i++;
        }

        if (typeof target !== "object" && !jQuery.isFunction(target)) {
            target = {};
        }

        if (i === length) {
            target = this;
            i--;
        }

        for (; i < length; i++) {
            if ((options = arguments[i]) != null) {
                for (name in options) {
                    src = target[name];
                    copy = options[name];

                    if (target === copy) {
                        continue;
                    }

                    if (deep && copy && (jQuery.isPlainObject(copy) || (copyIsArray = Array.isArray(copy)))) {
                        if (copyIsArray) {
                            copyIsArray = false;
                            clone = src && Array.isArray(src) ? src : [];

                        } else {
                            clone = src && jQuery.isPlainObject(src) ? src : {};
                        }

                        target[name] = jQuery.extend(deep, clone, copy);

                    } else if (copy !== undefined) {
                        target[name] = copy;
                    }
                }
            }
        }

        return target;
    };

    jQuery.extend({
        expando: "jQuery" + ("2.1.1" + Math.random()).replace(/\D/g, ""),
        isReady: true,
        error: function (msg: string): never {
            throw new Error(msg);
        },
        noop: function (): void {},
        isFunction: function (obj: any): obj is Function {
            return jQuery.type(obj) === "function";
        },
        isArray: Array.isArray,
        isWindow: function (obj: any): obj is Window {
            return obj != null && obj === obj.window;
        },
        isNumeric: function (obj: any): boolean {
            return !jQuery.isArray(obj) && obj - parseFloat(obj) >= 0;
        },
        isPlainObject: function (obj: any): obj is PlainObject {
            if (jQuery.type(obj) !== "object" || obj.nodeType || jQuery.isWindow(obj)) {
                return false;
            }
            if (obj.constructor && !Object.prototype.hasOwnProperty.call(obj.constructor.prototype, "isPrototypeOf")) {
                return false;
            }
            return true;
        },
        isEmptyObject: function (obj: any): boolean {
            for (const name in obj) {
                return false;
            }
            return true;
        },
        type: function (obj: any): string {
            if (obj == null) {
                return obj + "";
            }
            return typeof obj === "object" || typeof obj === "function"
                ? Object.prototype.toString.call(obj).slice(8, -1).toLowerCase()
                : typeof obj;
        },
        globalEval: function (code: string): void {
            const script: HTMLScriptElement = document.createElement("script");
            script.text = code;
            document.head.appendChild(script).parentNode!.removeChild(script);
        },
        camelCase: function (str: string): string {
            return str.replace(/^-ms-/, "ms-").replace(/-([\da-z])/gi, function (all, letter) {
                return letter.toUpperCase();
            });
        },
        nodeName: function (elem: Node, name: string): boolean {
            return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();
        },
        each: function (obj: any, callback: any): any {
            let length, i = 0;

            if (Array.isArray(obj)) {
                length = obj.length;
                for (; i < length; i++) {
                    if (callback.call(obj[i], i, obj[i]) === false) {
                        break;
                    }
                }
            } else {
                for (i in obj) {
                    if (callback.call(obj[i], i, obj[i]) === false) {
                        break;
                    }
                }
            }

            return obj;
        },
        trim: function (text: string): string {
            return text == null ? "" : (text + "").replace(/^\s+|\s+$/g, "");
        },
        makeArray: function (arr: any, results: any): any {
            const ret = results || [];

            if (arr != null) {
                if (Array.isArray(Object(arr))) {
                    jQuery.merge(ret, typeof arr === "string" ? [arr] : arr);
                } else {
                    ret.push(arr);
                }
            }

            return ret;
        },
        inArray: function (elem: any, arr: any, i: any): any {
            return arr == null ? -1 : arr.indexOf(elem, i);
        },
        merge: function (first: any, second: any): any {
            let len = +second.length,
                j = 0,
                i = first.length;

            for (; j < len; j++) {
                first[i++] = second[j];
            }

            first.length = i;

            return first;
        },
        grep: function (elems: any, callback: any, invert: any): any {
            const callbackInverse: any,
                matches: any = [],
                i = 0,
                length = elems.length,
                callbackExpect = !invert;

            for (; i < length; i++) {
                callbackInverse = !callback(elems[i], i);
                if (callbackInverse !== callbackExpect) {
                    matches.push(elems[i]);
                }
            }

            return matches;
        },
        map: function (elems: any, callback: any, arg: any): any {
            let length, value,
                i = 0,
                ret = [];

            if (Array.isArray(elems)) {
                length = elems.length;
                for (; i < length; i++) {
                    value = callback(elems[i], i, arg);

                    if (value != null) {
                        ret.push(value);
                    }
                }
            } else {
                for (i in elems) {
                    value = callback(elems[i], i, arg);

                    if (value != null) {
                        ret.push(value);
                    }
                }
            }

            return [].concat.apply([], ret);
        },
        guid: 1,
        proxy: function (fn: Function, context: any): Function {
            let tmp: any, args: any, proxy: any;

            if (typeof context === "string") {
                tmp = fn[context];
                context = fn;
                fn = tmp;
            }

            if (!jQuery.isFunction(fn)) {
                return undefined;
            }

            args = [].slice.call(arguments, 2);
            proxy = function (): any {
                return fn.apply(context || this, args.concat([].slice.call(arguments)));
            };

            proxy.guid = fn.guid = fn.guid || jQuery.guid++;

            return proxy;
        },
        now: Date.now,
        support: {}
    });

    jQuery.fn = rootjQuery;

    jQuery.extend = jQuery.fn.extend = function (): any {
        let options: any, name: any, src: any, copy: any, copyIsArray: any, clone: any,
            target = arguments[0] || {},
            i = 1,
            length = arguments.length,
            deep = false;

        if (typeof target === "boolean") {
            deep = target;
            target = arguments[i] || {};
            i++;
        }

        if (typeof target !== "object" && !jQuery.isFunction(target)) {
            target = {};
        }

        if (i === length) {
            target = this;
            i--;
        }

        for (; i < length; i++) {
            if ((options = arguments[i]) != null) {
                for (name in options) {
                    src = target[name];
                    copy = options[name];

                    if (target === copy) {
                        continue;
                    }

            if (deep && copy && (jQuery.isPlainObject(copy) || (copyIsArray = Array.isArray(copy)))) {
                if (copyIsArray) {
                    copyIsArray = false;
                    clone = src && Array.isArray(src) ? src : [];
                } else {
                    clone = src && jQuery.isPlainObject(src) ? src : {};
                }

                target[name] = jQuery.extend(deep, clone, copy);
            } else if (copy !== undefined) {
                target[name] = copy;
            }
        }
    }
}

return target;
};

jQuery.extend({
    expando: "jQuery" + ("2.1.1" + Math.random()).replace(/\D/g, ""),
    isReady: true,
    error: function (msg: string): never {
        throw new Error(msg);
    },
    noop: function (): void {},
    isFunction: function (obj: any): obj is Function {
        return jQuery.type(obj) === "function";
    },
    isArray: Array.isArray,
    isWindow: function (obj: any): obj is Window {
        return obj != null && obj === obj.window;
    },
    isNumeric: function (obj: any): boolean {
        return !jQuery.isArray(obj) && obj - parseFloat(obj) >= 0;
    },
    isPlainObject: function (obj: any): obj is PlainObject {
        if (jQuery.type(obj) !== "object" || obj.nodeType || jQuery.isWindow(obj)) {
            return false;
        }
        if (obj.constructor && !Object.prototype.hasOwnProperty.call(obj.constructor.prototype, "isPrototypeOf")) {
            return false;
        }
        return true;
    },
    isEmptyObject: function (obj: any): boolean {
        for (const name in obj) {
            return false;
        }
        return true;
    },
    type: function (obj: any): string {
        if (obj == null) {
            return obj + "";
        }
        return typeof obj === "object" || typeof obj === "function"
            ? Object.prototype.toString.call(obj).slice(8, -1).toLowerCase()
            : typeof obj;
    },
    globalEval: function (code: string): void {
        const script: HTMLScriptElement = document.createElement("script");
        script.text = code;
        document.head.appendChild(script).parentNode!.removeChild(script);
    },
    camelCase: function (str: string): string {
        return str.replace(/^-ms-/, "ms-").replace(/-([\da-z])/gi, function (all, letter) {
            return letter.toUpperCase();
        });
    },
    nodeName: function (elem: Node, name: string): boolean {
        return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();
    },
    each: function (obj: any, callback: any): any {
        let length, i = 0;

        if (Array.isArray(obj)) {
            length = obj.length;
            for (; i < length; i++) {
                if (callback.call(obj[i], i, obj[i]) === false) {
                    break;
                }
            }
        } else {
            for (i in obj) {
                if (callback.call(obj[i], i, obj[i]) === false) {
                    break;
                }
            }
        }

        return obj;
    },
    trim: function (text: string): string {
        return text == null ? "" : (text + "").replace(/^\s+|\s+$/g, "");
    },
    makeArray: function (arr: any, results: any): any {
        const ret = results || [];

        if (arr != null) {
            if (Array.isArray(Object(arr))) {
                jQuery.merge(ret, typeof arr === "string" ? [arr] : arr);
            } else {
                ret.push(arr);
            }
        }

        return ret;
    },
    inArray: function (elem: any, arr: any, i: any): any {
        return arr == null ? -1 : arr.indexOf(elem, i);
    },
    merge: function (first: any, second: any): any {
        let len = +second.length,
            j = 0,
            i = first.length;

        for (; j < len; j++) {
            first[i++] = second[j];
        }

        first.length = i;

        return first;
    },
    grep: function (elems: any, callback: any, invert: any): any {
        const callbackInverse: any,
            matches: any = [],
            i = 0,
            length = elems.length,
            callbackExpect = !invert;

        for (; i < length; i++) {
            callbackInverse = !callback(elems[i], i);
            if (callbackInverse !== callbackExpect) {
                matches.push(elems[i]);
            }
        }

        return matches;
    },
    map: function (elems: any, callback: any, arg: any): any {
        let length, value,
            i = 0,
            ret = [];

        if (Array.isArray(elems)) {
            length = elems.length;
            for (; i < length; i++) {
                value = callback(elems[i], i, arg);

                if (value != null) {
                    ret.push(value);
                }
            }
        } else {
            for (i in elems) {
                value = callback(elems[i], i, arg);

                if (value != null) {
                    ret.push(value);
                }
            }
        }

        return [].concat.apply([], ret);
    },
    guid: 1,
    proxy: function (fn: Function, context: any): Function {
        let tmp: any, args: any, proxy: any;

        if (typeof context === "string") {
            tmp = fn[context];
            context = fn;
            fn = tmp;
        }

        if (!jQuery.isFunction(fn)) {
            return undefined;
        }

        args = [].slice.call(arguments, 2);
        proxy = function (): any {
            return fn.apply(context || this, args.concat([].slice.call(arguments)));
        };

        proxy.guid = fn.guid = fn.guid || jQuery.guid++;

        return proxy;
    },
    now: Date.now,
    support: {}
});

jQuery.fn = rootjQuery;

jQuery.extend = jQuery.fn.extend = function (): any {
    let options: any, name: any, src: any, copy: any, copyIsArray: any, clone: any,
        target = arguments[0] || {},
        i = 1,
        length = arguments.length,
        deep = false;

    if (typeof target === "boolean") {
        deep = target;
        target = arguments[i] || {};
        i++;
    }

    if (typeof target !== "object" && !jQuery.isFunction(target)) {
        target = {};
    }

    if (i === length) {
        target = this;
        i--;
    }

    for (; i < length; i++) {
        if ((options = arguments[i]) != null) {
            for (name in options) {
                src = target[name];
                copy = options[name];

                if (target === copy) {
                    continue;
                }

                if (deep && copy && (jQuery.isPlainObject(copy) || (copyIsArray = Array.isArray(copy)))) {
                    if (copyIsArray) {
                        copyIsArray = false;
                        clone = src && Array.isArray(src) ? src : [];

                    } else {
                        clone = src && jQuery.isPlainObject(src) ? src : {};
                    }

                    target[name] = jQuery.extend(deep, clone, copy);

                } else if (copy !== undefined) {
                    target[name] = copy;
                }
            }
        }
    }

    return target;
};

jQuery.extend({
    expando: "jQuery" + ("2.1.1" + Math.random()).replace(/\D/g, ""),
    isReady: true,
    error: function (msg: string): never {
        throw new Error(msg);
    },
    noop: function (): void {},
    isFunction: function (obj: any): obj is Function {
        return jQuery.type(obj) === "function";
    },
    isArray: Array.isArray,
    isWindow: function (obj: any): obj is Window {
        return obj != null && obj === obj.window;
    },
    isNumeric: function (obj: any): boolean {
        return !jQuery.isArray(obj) && obj - parseFloat(obj) >= 0;
    },
    isPlainObject: function (obj: any): obj is PlainObject {
        if (jQuery.type(obj) !== "object" || obj.nodeType || jQuery.isWindow(obj)) {
            return false;
        }
        if (obj.constructor && !Object.prototype.hasOwnProperty.call(obj.constructor.prototype, "isPrototypeOf")) {
            return false;
        }
        return true;
    },
    isEmptyObject: function (obj: any): boolean {
        for (const name in obj) {
            return false;
        }
        return true;
    },
    type: function (obj: any): string {
        if (obj == null) {
            return obj + "";
        }
        return typeof obj === "object" || typeof obj === "function"
            ? Object.prototype.toString.call(obj).slice(8, -1).toLowerCase()
            : typeof obj;
    },
    globalEval: function (code: string): void {
        const script: HTMLScriptElement = document.createElement("script");
        script.text = code;
        document.head.appendChild(script).parentNode!.removeChild(script);
    },
    camelCase: function (str: string): string {
        return str.replace(/^-ms-/, "ms-").replace(/-([\da-z])/gi, function (all, letter) {
            return letter.toUpperCase();
        });
    },
    nodeName: function (elem: Node, name: string): boolean {
        return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();
    },
    each: function (obj: any, callback: any): any {
        let length, i = 0;

        if (Array.isArray(obj)) {
            length = obj.length;
            for (; i < length; i++) {
                if (callback.call(obj[i], i, obj[i]) === false) {
                    break;
                }
            }
        } else {
            for (i in obj) {
                if (callback.call(obj[i], i, obj[i]) === false) {
                    break;
                }
            }
        }

        return obj;
    },
    trim: function (text: string): string {
        return text == null ? "" : (text + "").replace(/^\s+|\s+$/g, "");
    },
    makeArray: function (arr: any, results: any): any {
        const ret = results || [];

        if (arr != null) {
            if (Array.isArray(Object(arr))) {
                jQuery.merge(ret, typeof arr === "string" ? [arr] : arr);
            } else {
                ret.push(arr);
            }
        }

        return ret;
    },
    inArray: function (elem: any, arr: any, i: any): any {
        return arr == null ? -1 : arr.indexOf(elem, i);
    },
    merge: function (first: any, second: any): any {
        let len = +second.length,
            j = 0,
            i = first.length;

        for (; j < len; j++) {
            first[i++] = second[j];
        }

        first.length = i;

        return first;
    },
    grep: function (elems: any, callback: any, invert: any): any {
        const callbackInverse: any,
            matches: any = [],
            i = 0,
            length = elems.length,
            callbackExpect = !invert;

        for (; i < length; i++) {
            callbackInverse = !callback(elems[i], i);
            if (callbackInverse !== callbackExpect) {
                matches.push(elems[i]);
            }
        }

        return matches;
    },
    map: function (elems: any, callback: any, arg: any): any {
        let length, value,
            i = 0,
            ret = [];

        if (Array.isArray(elems)) {
            length = elems.length;
            for (; i < length; i++) {
                value = callback(elems[i], i, arg);

                if (value != null) {
                    ret.push(value);
                }
            }
        } else {
            for (i in elems) {
                value = callback(elems[i], i, arg);

                if (value != null) {
                    ret.push(value);
                }
            }
        }

        return [].concat.apply([], ret);
    },
    guid: 1,
    proxy: function (fn: Function, context: any): Function {
        let tmp: any, args: any, proxy: any;

        if (typeof context === "string") {
            tmp = fn[context];
            context = fn;
            fn = tmp;
        }

        if (!jQuery.isFunction(fn)) {
            return undefined;
        }

        args = [].slice.call(arguments, 2);
        proxy = function (): any {
            return fn.apply(context || this, args.concat([].slice.call(arguments)));
        };

        proxy.guid = fn.guid = fn.guid || jQuery.guid++;

        return proxy;
    },
    now: Date.now,
    support: {}
});

jQuery.fn = rootjQuery;

jQuery.extend = jQuery.fn.extend = function (): any {
    let options: any, name: any, src: any, copy: any, copyIsArray: any, clone: any,
        target = arguments[0] || {},
        i = 1,
        length = arguments.length,
        deep = false;

    if (typeof target === "boolean") {
        deep = target;
        target = arguments[i] || {};
        i++;
    }

    if (typeof target !== "object" && !jQuery.isFunction(target)) {
        target = {};
    }

    if (i === length) {
        target = this;
        i--;
    }

    for (; i < length; i++) {
        if ((options = arguments[i]) != null) {
            for (name in options) {
                src = target[name];
                copy = options[name];

                if (target === copy) {
                    continue;
                }

                if (deep && copy && (jQuery.isPlainObject(copy) || (copyIsArray = Array.isArray(copy)))) {
                    if (copyIsArray) {
                        copyIsArray = false;
                        clone = src && Array.isArray(src) ? src : [];

                    } else {
                        clone = src && jQuery.isPlainObject(src) ? src : {};
                    }

                    target[name] = jQuery.extend(deep, clone, copy);

                } else if (copy !== undefined) {
                    target[name] = copy;
                }
            }
        }
    }

    return target;
};

jQuery.extend({
    expando: "jQuery" + ("2.1.1" + Math.random()).replace(/\D/g, ""),
    isReady: true,
    error: function (msg: string): never {
        throw new Error(msg);
    },
    noop: function (): void {},
    isFunction: function (obj: any): obj is Function {
        return jQuery.type(obj) === "function";
    },
    isArray: Array.isArray,
    isWindow: function (obj: any): obj is Window {
        return obj != null && obj === obj.window;
    },
    isNumeric: function (obj: any): boolean {
        return !jQuery.isArray(obj) && obj - parseFloat(obj) >= 0;
    },
    isPlainObject: function (obj: any): obj is PlainObject {
        if (jQuery.type(obj) !== "object" || obj.nodeType || jQuery.isWindow(obj)) {
            return false;
        }
        if (obj.constructor && !Object.prototype.hasOwnProperty.call(obj.constructor.prototype, "isPrototypeOf")) {
            return false;
        }
        return true;
    },
    isEmptyObject: function (obj: any): boolean {
        for (const name in obj) {
            return false;
        }
        return true;
    },
    type: function (obj: any): string {
        if (obj == null) {
            return obj + "";
        }
        return typeof obj === "object" || typeof obj === "function"
            ? Object.prototype.toString.call(obj).slice(8, -1).toLowerCase()
            : typeof obj;
    },
    globalEval: function (code: string): void {
        const script: HTMLScriptElement = document.createElement("script");
        script.text = code;
        document.head.appendChild(script).parentNode!.removeChild(script);
    },
    camelCase: function (str: string): string {
        return str.replace(/^-ms-/, "ms-").replace(/-([\da-z])/gi, function (all, letter) {
            return letter.toUpperCase();
        });
    },
    nodeName: function (elem: Node, name: string): boolean {
        return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();
    },
    each: function (obj: any, callback: any): any {
        let length, i = 0;

        if (Array.isArray(obj)) {
            length = obj.length;
            for (; i < length; i++) {
                if (callback.call(obj[i], i, obj[i]) === false) {
                    break;
                }
            }
        } else {
            for (i in obj) {
                if (callback.call(obj[i], i, obj[i]) === false) {
                    break;
                }
            }
        }

        return obj;
    },
    trim: function (text: string): string {
        return text == null ? "" : (text + "").replace(/^\s+|\s+$/g, "");
    },
    makeArray: function (arr: any, results: any): any {
        const ret = results || [];

        if (arr != null) {
            if (Array.isArray(Object(arr))) {
                jQuery.merge(ret, typeof arr === "string" ? [arr] : arr);
            } else {
                ret.push(arr);
            }
        }

        return ret;
    },
    inArray: function (elem: any, arr: any, i: any): any {
        return arr == null ? -1 : arr.indexOf(elem, i);
    },
    merge: function (first: any, second: any): any {
        let len = +second.length,
            j = 0,
            i = first.length;

        for (; j < len; j++) {
            first[i++] = second[j];
        }

        first.length = i;

        return first;
    },
    grep: function (elems: any, callback: any, invert: any): any {
        const callbackInverse: any,
            matches: any = [],
            i = 0,
            length = elems.length,
            callbackExpect = !invert;

        for (; i < length; i++) {
            callbackInverse = !callback(elems[i], i);
            if (callbackInverse !== callbackExpect) {
                matches.push(elems[i]);
            }
       matches.push(elems[i]);
            }
        }

        return matches;
    },
    map: function (elems: any, callback: any, arg: any): any {
        let length, value,
            i = 0,
            ret = [];

        if (Array.isArray(elems)) {
            length = elems.length;
            for (; i < length; i++) {
                value = callback(elems[i], i, arg);

                if (value != null) {
                    ret.push(value);
                }
            }
        } else {
            for (i in elems) {
                value = callback(elems[i], i, arg);

                if (value != null) {
                    ret.push(value);
                }
            }
        }

        return [].concat.apply([], ret);
    },
    guid: 1,
    proxy: function (fn: Function, context: any): Function {
        let tmp: any, args: any, proxy: any;

        if (typeof context === "string") {
            tmp = fn[context];
            context = fn;
            fn = tmp;
        }

        if (!jQuery.isFunction(fn)) {
            return undefined;
        }

        args = [].slice.call(arguments, 2);
        proxy = function (): any {
            return fn.apply(context || this, args.concat([].slice.call(arguments)));
        };

        proxy.guid = fn.guid = fn.guid || jQuery.guid++;

        return proxy;
    },
    now: Date.now,
    support: {}
});

jQuery.fn = rootjQuery;

jQuery.extend = jQuery.fn.extend = function (): any {
    let options: any, name: any, src: any, copy: any, copyIsArray: any, clone: any,
        target = arguments[0] || {},
        i = 1,
        length = arguments.length,
        deep = false;

    if (typeof target === "boolean") {
        deep = target;
        target = arguments[i] || {};
        i++;
    }

    if (typeof target !== "object" && !jQuery.isFunction(target)) {
        target = {};
    }

    if (i === length) {
        target = this;
        i--;
    }

    for (; i < length; i++) {
        if ((options = arguments[i]) != null) {
            for (name in options) {
                src = target[name];
                copy = options[name];

                if (target === copy) {
                    continue;
                }

                if (deep && copy && (jQuery.isPlainObject(copy) || (copyIsArray = Array.isArray(copy)))) {
                    if (copyIsArray) {
                        copyIsArray = false;
                        clone = src && Array.isArray(src) ? src : [];

                    } else {
                        clone = src && jQuery.isPlainObject(src) ? src : {};
                    }

                    target[name] = jQuery.extend(deep, clone, copy);

                } else if (copy !== undefined) {
                    target[name] = copy;
                }
            }
        }
    }

    return target;
};

jQuery.extend({
    expando: "jQuery" + ("2.1.1" + Math.random()).replace(/\D/g, ""),
    isReady: true,
    error: function (msg: string): never {
        throw new Error(msg);
    },
    noop: function (): void {},
    isFunction: function (obj: any): obj is Function {
        return jQuery.type(obj) === "function";
    },
    isArray: Array.isArray,
    isWindow: function (obj: any): obj is Window {
        return obj != null && obj === obj.window;
    },
    isNumeric: function (obj: any): boolean {
        return !jQuery.isArray(obj) && obj - parseFloat(obj) >= 0;
    },
    isPlainObject: function (obj: any): obj is PlainObject {
        if (jQuery.type(obj) !== "object" || obj.nodeType || jQuery.isWindow(obj)) {
            return false;
        }
        if (obj.constructor && !Object.prototype.hasOwnProperty.call(obj.constructor.prototype, "isPrototypeOf")) {
            return false;
        }
        return true;
    },
    isEmptyObject: function (obj: any): boolean {
        for (const name in obj) {
            return false;
        }
        return true;
    },
    type: function (obj: any): string {
        if (obj == null) {
            return obj + "";
        }
        return typeof obj === "object" || typeof obj === "function"
            ? Object.prototype.toString.call(obj).slice(8, -1).toLowerCase()
            : typeof obj;
    },
    globalEval: function (code: string): void {
        const script: HTMLScriptElement = document.createElement("script");
        script.text = code;
        document.head.appendChild(script).parentNode!.removeChild(script);
    },
    camelCase: function (str: string): string {
        return str.replace(/^-ms-/, "ms-").replace(/-([\da-z])/gi, function (all, letter) {
            return letter.toUpperCase();
        });
    },
    nodeName: function (elem: Node, name: string): boolean {
        return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();
    },
    each: function (obj: any, callback: any): any {
        let length, i = 0;

        if (Array.isArray(obj)) {
            length = obj.length;
            for (; i < length; i++) {
                if (callback.call(obj[i], i, obj[i]) === false) {
                    break;
                }
            }
        } else {
            for (i in obj) {
                if (callback.call(obj[i], i, obj[i]) === false) {
                    break;
                }
            }
        }

        return obj;
    },
    trim: function (text: string): string {
        return text == null ? "" : (text + "").replace(/^\s+|\s+$/g, "");
    },
    makeArray: function (arr: any, results: any): any {
        const ret = results || [];

        if (arr != null) {
            if (Array.isArray(Object(arr))) {
                jQuery.merge(ret, typeof arr === "string" ? [arr] : arr);
            } else {
                ret.push(arr);
            }
        }

        return ret;
    },
    inArray: function (elem: any, arr: any, i: any): any {
        return arr == null ? -1 : arr.indexOf(elem, i);
    },
    merge: function (first: any, second: any): any {
        let len = +second.length,
            j = 0,
            i = first.length;

        for (; j < len; j++) {
            first[i++] = second[j];
        }

        first.length = i;

        return first;
    },
    grep: function (elems: any, callback: any, invert: any): any {
        const callbackInverse: any,
            matches: any = [],
            i = 0,
            length = elems.length,
            callbackExpect = !invert;

        for (; i < length; i++) {
            callbackInverse = !callback(elems[i], i);
            if (callbackInverse !== callbackExpect) {
                matches.push(elems[i]);
            }
        }

        return matches;
    },
    map: function (elems: any, callback: any, arg: any): any {
        let length, value,
            i = 0,
            ret = [];

        if (Array.isArray(elems)) {
            length = elems.length;
            for (; i < length; i++) {
                value = callback(elems[i], i, arg);

                if (value != null) {
                    ret.push(value);
                }
            }
        } else {
            for (i in elems) {
                value = callback(elems[i], i, arg);

                if (value != null) {
                    ret.push(value);
                }
            }
        }

        return [].concat.apply([], ret);
    },
    guid: 1,
    proxy: function (fn: Function, context: any): Function {
        let tmp: any, args: any, proxy: any;

        if (typeof context === "string") {
            tmp = fn[context];
            context = fn;
            fn = tmp;
        }

        if (!jQuery.isFunction(fn)) {
            return undefined;
        }

        args = [].slice.call(arguments, 2);
        proxy = function (): any {
            return fn.apply(context || this, args.concat([].slice.call(arguments)));
        };

        proxy.guid = fn.guid = fn.guid || jQuery.guid++;

        return proxy;
    },
    now: Date.now,
    support: {}
});

jQuery.fn = rootjQuery;

jQuery.extend = jQuery.fn.extend = function (): any {
    let options: any, name: any, src: any, copy: any, copyIsArray: any, clone: any,
        target = arguments[0] || {},
        i = 1,
        length = arguments.length,
        deep = false;

    if (typeof target === "boolean") {
        deep = target;
        target = arguments[i] || {};
        i++;
    }

    if (typeof target !== "object" && !jQuery.isFunction(target)) {
        target = {};
    }

    if (i === length) {
        target = this;
        i--;
    }

    for (; i < length; i++) {
        if ((options = arguments[i]) != null) {
            for (name in options) {
                src = target[name];
                copy = options[name];

                if (target === copy) {
                    continue;
                }

                if (deep && copy && (jQuery.isPlainObject(copy) || (copyIsArray = Array.isArray(copy)))) {
                    if (copyIsArray) {
                        copyIsArray = false;
                        clone = src && Array.isArray(src) ? src : [];

                    } else {
                        clone = src && jQuery.isPlainObject(src) ? src : {};
                    }

                    target[name] = jQuery.extend(deep, clone, copy);

                } else if (copy !== undefined) {
                    target[name] = copy;
                }
            }
        }
    }

    return target;
};

jQuery.extend({
    expando: "jQuery" + ("2.1.1" + Math.random()).replace(/\D/g, ""),
    isReady: true,
    error: function (msg: string): never {
        throw new Error(msg);
    },
    noop: function (): void {},
    isFunction: function (obj: any): obj is Function {
        return jQuery.type(obj) === "function";
    },
    isArray: Array.isArray,
    isWindow: function (obj: any): obj is Window {
        return obj != null && obj === obj.window;
    },
    isNumeric: function (obj: any): boolean {
        return !jQuery.isArray(obj) && obj - parseFloat(obj) >= 0;
    },
    isPlainObject: function (obj: any): obj is PlainObject {
        if (jQuery.type(obj) !== "object" || obj.nodeType || jQuery.isWindow(obj)) {
            return false;
        }
        if (obj.constructor && !Object.prototype.hasOwnProperty.call(obj.constructor.prototype, "isPrototypeOf")) {
            return false;
        }
        return true;
    },
    isEmptyObject: function (obj: any): boolean {
        for (const name in obj) {
            return false;
        }
        return true;
    },
    type: function (obj: any): string {
        if (obj == null) {
            return obj + "";
        }
        return typeof obj === "object" || typeof obj === "function"
            ? Object.prototype.toString.call(obj).slice(8, -1).toLowerCase()
            : typeof obj;
    },
    globalEval: function (code: string): void {
        const script: HTMLScriptElement = document.createElement("script");
        script.text = code;
        document.head.appendChild(script).parentNode!.removeChild(script);
    },
    camelCase: function (str: string): string {
        return str.replace(/^-ms-/, "ms-").replace(/-([\da-z])/gi, function (all, letter) {
            return letter.toUpperCase();
        });
    },
    nodeName: function (elem: Node, name: string): boolean {
        return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();
    },
    each: function (obj: any, callback: any): any {
        let length, i = 0;

        if (Array.isArray(obj)) {
            length = obj.length;
            for (; i < length; i++) {
                if (callback.call(obj[i], i, obj[i]) === false) {
                    break;
                }
            }
        } else {
            for (i in obj) {
                if (callback.call(obj[i], i, obj[i]) === false) {
                    break;
                }
            }
        }

        return obj;
    },
    trim: function (text: string): string {
        return text == null ? "" : (text + "").replace(/^\s+|\s+$/g, "");
    },
    makeArray: function (arr: any, results: any): any {
        const ret = results || [];

        if (arr != null) {
            if (Array.isArray(Object(arr))) {
                jQuery.merge(ret, typeof arr === "string" ? [arr] : arr);
            } else {
                ret.push(arr);
            }
        }

        return ret;
    },
    inArray: function (elem: any, arr: any, i: any): any {
        return arr == null ? -1 : arr.indexOf(elem, i);
    },
    merge: function (first: any, second: any): any {
        let len = +second.length,
            j = 0,
            i = first.length;

        for (; j < len; j++) {
            first[i++] = second[j];
        }

        first.length = i;

        return first;
    },
    grep: function (elems: any, callback: any, invert: any): any {
        const callbackInverse: any,
            matches: any = [],
            i = 0,
            length = elems.length,
            callbackExpect = !invert;

        for (; i < length; i++) {
            callbackInverse = !callback(elems[i], i);
            if (callbackInverse !== callbackExpect) {
                matches.push(elems[i]);
            }
        }

        return matches;
    },
    map: function (elems: any, callback: any, arg: any): any {
        let length, value,
            i = 0,
            ret = [];

        if (Array.isArray(elems)) {
            length = elems.length;
            for (; i < length; i++) {
                value = callback(elems[i], i, arg);

                if (value != null) {
                    ret.push(value);
                }
            }
        } else {
            for (i in elems) {
                value = callback(elems[i], i, arg);

                if (value != null) {
                    ret.push(value);
                }
            }
        }

        return [].concat.apply([], ret);
    },
    guid: 1,
    proxy: function (fn: Function, context: any): Function {
        let tmp: any, args: any, proxy: any;

        if (typeof context === "string") {
            tmp = fn[context];
            context = fn;
            fn = tmp;
        }

        if (!jQuery.isFunction(fn)) {
            return undefined;
        }

        args = [].slice.call(arguments, 2);
        proxy = function (): any {
            return fn.apply(context || this, args.concat([].slice.call(arguments)));
        };

        proxy.guid = fn.guid = fn.guid || jQuery.guid++;

        return proxy;
    },
    now: Date.now,
    support: {}
});

jQuery.fn = rootjQuery;

jQuery.extend = jQuery.fn.extend = function (): any {
    let options: any, name: any, src: any, copy: any, copyIsArray: any, clone: any,
        target = arguments[0] || {},
        i = 1,
        length = arguments.length,
        deep = false;

    if (typeof target === "boolean") {
        deep = target;
        target = arguments[i] || {};
        i++;
    }

    if (typeof target !== "object" && !jQuery.isFunction(target)) {
        target = {};
    }

    if (i === length) {
        target = this;
        i--;
    }

    for (; i < length; i++) {
        if ((options = arguments[i]) != null) {
            for (name in options) {
                src = target[name];
                copy = options[name];

                if (target === copy) {
                    continue;
                }

                if (deep && copy && (jQuery.isPlainObject(copy) || (copyIsArray = Array.isArray(copy)))) {
                    if (copyIsArray) {
                        copyIsArray = false;
                        clone = src && Array.isArray(src) ? src : [];

                    } else {
                        clone = src && jQuery.isPlainObject(src) ? src : {};
                    }

                    target[name] = jQuery.extend(deep, clone, copy);

                } else if (copy !== undefined) {
                    target[name] = copy;
                }
            }
        }
    }

    return target;
};

jQuery.extend({
    expando: "jQuery" + ("2.1.1" + Math.random()).replace(/\D/g, ""),
    isReady: true,
    error: function (msg: string): never {
        throw new Error(msg);
    },
    noop: function (): void {},
    isFunction: function (obj: any): obj is Function {
        return jQuery.type(obj) === "function";
    },
    isArray: Array.isArray,
    isWindow: function (obj: any): obj is Window {
        return obj != null && obj === obj.window;
    },
    isNumeric: function (obj: any): boolean {
        return !jQuery.isArray(obj) && obj - parseFloat(obj) >= 0;
    },
    isPlainObject: function (obj: any): obj is PlainObject {
        if (jQuery.type(obj) !== "object" || obj.nodeType || jQuery.isWindow(obj)) {
            return false;
        }
        if (obj.constructor && !Object.prototype.hasOwnProperty.call(obj.constructor.prototype, "isPrototypeOf")) {
            return false;
        }
        return true;
    },
    isEmptyObject: function (obj: any): boolean {
        for (const name in obj) {
            return false;
        }
        return true;
    },
    type: function (obj: any): string {
        if (obj == null) {
            return obj + "";
        }
        return typeof obj === "object" || typeof obj === "function"
            ? Object.prototype.toString.call(obj).slice(8, -1).toLowerCase()
            : typeof obj;
    },
    globalEval: function (code: string): void {
        const script: HTMLScriptElement = document.createElement("script");
        script.text = code;
        document.head.appendChild(script).parentNode!.removeChild(script);
    },
        camelCase: function (str: string): string {
            return str.replace(/^-ms-/, "ms-").replace(/-([\da-z])/gi, function (all, letter) {
                return letter.toUpperCase();
            });
        },
        nodeName: function (elem: Node, name: string): boolean {
            return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();
        },
        each: function (obj: any, callback: any): any {
            let length, i = 0;

            if (Array.isArray(obj)) {
                length = obj.length;
                for (; i < length; i++) {
                    if (callback.call(obj[i], i, obj[i]) === false) {
                        break;
                    }
                }
            } else {
                for (i in obj) {
                    if (callback.call(obj[i], i, obj[i]) === false) {
                        break;
                    }
                }
            }

            return obj;
        },
        trim: function (text: string): string {
            return text == null ? "" : (text + "").replace(/^\s+|\s+$/g, "");
        },
        makeArray: function (arr: any, results: any): any {
            const ret = results || [];

            if (arr != null) {
                if (Array.isArray(Object(arr))) {
                    jQuery.merge(ret, typeof arr === "string" ? [arr] : arr);
                } else {
                    ret.push(arr);
                }
            }

            return ret;
        },
        inArray: function (elem: any, arr: any, i: any): any {
            return arr == null ? -1 : arr.indexOf(elem, i);
        },
        merge: function (first: any, second: any): any {
            let len = +second.length,
                j = 0,
                i = first.length;

            for (; j < len; j++) {
                first[i++] = second[j];
            }

            first.length = i;

            return first;
        },
        grep: function (elems: any, callback: any, invert: any): any {
            const callbackInverse: any,
                matches: any = [],
                i = 0,
                length = elems.length,
                callbackExpect = !invert;

            for (; i < length; i++) {
                callbackInverse = !callback(elems[i], i);
                if (callbackInverse !== callbackExpect) {
                    matches.push(elems[i]);
                }
            }

            return matches;
        },
        map: function (elems: any, callback: any, arg: any): any {
            let length, value,
                i = 0,
                ret = [];

            if (Array.isArray(elems)) {
                length = elems.length;
                for (; i < length; i++) {
                    value = callback(elems[i], i, arg);

                    if (value != null) {
                        ret.push(value);
                    }
                }
            } else {
                for (i in elems) {
                    value = callback(elems[i], i, arg);

                    if (value != null) {
                        ret.push(value);
                    }
                }
            }

            return [].concat.apply([], ret);
        },
        guid: 1,
        proxy: function (fn: Function, context: any): Function {
            let tmp: any, args: any, proxy: any;

            if (typeof context === "string") {
                tmp = fn[context];
                context = fn;
                fn = tmp;
            }

            if (!jQuery.isFunction(fn)) {
                return undefined;
            }

            args = [].slice.call(arguments, 2);
            proxy = function (): any {
                return fn.apply(context || this, args.concat([].slice.call(arguments)));
            };

            proxy.guid = fn.guid = fn.guid || jQuery.guid++;

            return proxy;
        },
        now: Date.now,
        support: {}
    });

    jQuery.fn = rootjQuery;

    jQuery.extend = jQuery.fn.extend = function (): any {
        let options: any, name: any, src: any, copy: any, copyIsArray: any, clone: any,
            target = arguments[0] || {},
            i = 1,
            length = arguments.length,
            deep = false;

        if (typeof target === "boolean") {
            deep = target;
            target = arguments[i] || {};
            i++;
        }

        if (typeof target !== "object" && !jQuery.isFunction(target)) {
            target = {};
        }

        if (i === length) {
            target = this;
            i--;
        }

        for (; i < length; i++) {
            if ((options = arguments[i]) != null) {
                for (name in options) {
                    src = target[name];
                    copy = options[name];

                    if (target === copy) {
                        continue;
                    }

                    if (deep && copy && (jQuery.isPlainObject(copy) || (copyIsArray = Array.isArray(copy)))) {
                        if (copyIsArray) {
                            copyIsArray = false;
                            clone = src && Array.isArray(src) ? src : [];

                        } else {
                            clone = src && jQuery.isPlainObject(src) ? src : {};
                        }

                        target[name] = jQuery.extend(deep, clone, copy);

                    } else if (copy !== undefined) {
                        target[name] = copy;
                    }
                }
            }
        }

        return target;
    };

    jQuery.extend({
        expando: "jQuery" + ("2.1.1" + Math.random()).replace(/\D/g, ""),
        isReady: true,
        error: function (msg: string): never {
            throw new Error(msg);
        },
        noop: function (): void {},
        isFunction: function (obj: any): obj is Function {
            return jQuery.type(obj) === "function";
        },
        isArray: Array.isArray,
        isWindow: function (obj: any): obj is Window {
            return obj != null && obj === obj.window;
        },
        isNumeric: function (obj: any): boolean {
            return !jQuery.isArray(obj) && obj - parseFloat(obj) >= 0;
        },
        isPlainObject: function (obj: any): obj is PlainObject {
            if (jQuery.type(obj) !== "object" || obj.nodeType || jQuery.isWindow(obj)) {
                return false;
            }
            if (obj.constructor && !Object.prototype.hasOwnProperty.call(obj.constructor.prototype, "isPrototypeOf")) {
                return false;
            }
            return true;
        },
        isEmptyObject: function (obj: any): boolean {
            for (const name in obj) {
                return false;
            }
            return true;
        },
        type: function (obj: any): string {
            if (obj == null) {
                return obj + "";
            }
            return typeof obj === "object" || typeof obj === "function"
                ? Object.prototype.toString.call(obj).slice(8, -1).toLowerCase()
                : typeof obj;
        },
        globalEval: function (code: string): void {
            const script: HTMLScriptElement = document.createElement("script");
            script.text = code;
            document.head.appendChild(script).parentNode!.removeChild(script);
        },
        camelCase: function (str: string): string {
            return str.replace(/^-ms-/, "ms-").replace(/-([\da-z])/gi, function (all, letter) {
                return letter.toUpperCase();
            });
        },
        nodeName: function (elem: Node, name: string): boolean {
            return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();
        },
        each: function (obj: any, callback: any): any {
            let length, i = 0;

            if (Array.isArray(obj)) {
                length = obj.length;
                for (; i < length; i++) {
                    if (callback.call(obj[i], i, obj[i]) === false) {
                        break;
                    }
                }
            } else {
                for (i in obj) {
                    if (callback.call(obj[i], i, obj[i]) === false) {
                        break;
                    }
                }
            }

            return obj;
        },
        trim: function (text: string): string {
            return text == null ? "" : (text + "").replace(/^\s+|\s+$/g, "");
        },
        makeArray: function (arr: any, results: any): any {
            const ret = results || [];

            if (arr != null) {
                if (Array.isArray(Object(arr))) {
                    jQuery.merge(ret, typeof arr === "string" ? [arr] : arr);
                } else {
                    ret.push(arr);
                }
            }

            return ret;
        },
        inArray: function (elem: any, arr: any, i: any): any {
            return arr == null ? -1 : arr.indexOf(elem, i);
        },
        merge: function (first: any, second: any): any {
            let len = +second.length,
                j = 0,
                i = first.length;

            for (; j < len; j++) {
                first[i++] = second[j];
            }

            first.length = i;

            return first;
        },
        grep: function (elems: any, callback: any, invert: any): any {
            const callbackInverse: any,
                matches: any = [],
                i = 0,
                length = elems.length,
                callbackExpect = !invert;

            for (; i < length; i++) {
                callbackInverse = !callback(elems[i], i);
                if (callbackInverse !== callbackExpect) {
                    matches.push(elems[i]);
                }
            }

            return matches;
        },
        map: function (elems: any, callback: any, arg: any): any {
            let length, value,
                i = 0,
                ret = [];

            if (Array.isArray(elems)) {
                length = elems.length;
                for (; i < length; i++) {
                    value = callback(elems[i], i, arg);

                    if (value != null) {
                        ret.push(value);
                    }
                }
            } else {
                for (i in elems) {
                    value = callback(elems[i], i, arg);

                    if (value != null) {
                        ret.push(value);
                    }
                }
            }

            return [].concat.apply([], ret);
        },
        guid: 1,
        proxy: function (fn: Function, context: any): Function {
            let tmp: any, args: any, proxy: any;

            if (typeof context === "string") {
                tmp = fn[context];
                context = fn;
                fn = tmp;
            }

            if (!jQuery.isFunction(fn)) {
                return undefined;
            }

            args = [].slice.call(arguments, 2);
            proxy = function (): any {
                return fn.apply(context || this, args.concat([].slice.call(arguments)));
            };

            proxy.guid = fn.guid = fn.guid || jQuery.guid++;

            return proxy;
        },
        now: Date.now,
        support: {}
    });

    jQuery.fn = rootjQuery;

    jQuery.extend = jQuery.fn.extend = function (): any {
        let options: any, name: any, src: any, copy: any, copyIsArray: any, clone: any,
            target = arguments[0] || {},
            i = 1,
            length = arguments.length,
            deep = false;

        if (typeof target === "boolean") {
            deep = target;
            target = arguments[i] || {};
            i++;
        }

        if (typeof target !== "object" && !jQuery.isFunction(target)) {
            target = {};
        }

        if (i === length) {
            target = this;
            i--;
        }

        for (; i < length; i++) {
            if ((options = arguments[i]) != null) {
                for (name in options) {
                    src = target[name];
                    copy = options[name];

                    if (target === copy) {
                        continue;
                    }

                    if (deep && copy && (jQuery.isPlainObject(copy) || (copyIsArray = Array.isArray(copy)))) {
                        if (copyIsArray) {
                            copyIsArray = false;
                            clone = src && Array.isArray(src) ? src : [];

                        } else {
                            clone = src && jQuery.isPlainObject(src) ? src : {};
                        }

                        target[name] = jQuery.extend(deep, clone, copy);

                    } else if (copy !== undefined) {
                        target[name] = copy;
                    }
                }
            }
        }

        return target;
    };

    jQuery.extend({
        expando: "jQuery" + ("2.1.1" + Math.random()).replace(/\D/g, ""),
        isReady: true,
        error: function (msg: string): never {
            throw new Error(msg);
        },
        noop: function (): void {},
        isFunction: function (obj: any): obj is Function {
            return jQuery.type(obj) === "function";
        },
        isArray: Array.isArray,
        isWindow: function (obj: any): obj is Window {
            return obj != null && obj === obj.window;
        },
        isNumeric: function (obj: any): boolean {
            return !jQuery.isArray(obj) && obj - parseFloat(obj) >= 0;
        },
        isPlainObject: function (obj: any): obj is PlainObject {
            if (jQuery.type(obj) !== "object" || obj.nodeType || jQuery.isWindow(obj)) {
                return false;
            }
            if (obj.constructor && !Object.prototype.hasOwnProperty.call(obj.constructor.prototype, "isPrototypeOf")) {
                return false;
            }
            return true;
        },
        isEmptyObject: function (obj: any): boolean {
            for (const name in obj) {
                return false;
            }
            return true;
        },
        type: function (obj: any): string {
            if (obj == null) {
                return obj + "";
            }
            return typeof obj === "object" || typeof obj === "function"
                ? Object.prototype.toString.call(obj).slice(8, -1).toLowerCase()
                : typeof obj;
        },
        globalEval: function (code: string): void {
            const script: HTMLScriptElement = document.createElement("script");
            script.text = code;
            document.head.appendChild(script).parentNode!.removeChild(script);
        },
        camelCase: function (str: string): string {
            return str.replace(/^-ms-/, "ms-").replace(/-([\da-z])/gi, function (all, letter) {
                return letter.toUpperCase();
            });
        },
        nodeName: function (elem: Node, name: string): boolean {
            return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();
        },
        each: function (obj: any, callback: any): any {
            let length, i = 0;

            if (Array.isArray(obj)) {
                length = obj.length;
                for (; i < length; i++) {
                    if (callback.call(obj[i], i, obj[i]) === false) {
                        break;
                    }
                }
            } else {
                for (i in obj) {
                    if (callback.call(obj[i], i, obj[i]) === false) {
                        break;
                    }
                }
            }

            return obj;
        },
        trim: function (text: string): string {
            return text == null ? "" : (text + "").replace(/^\s+|\s+$/g, "");
        },
        makeArray: function (arr: any, results: any): any {
            const ret = results || [];

            if (arr != null) {
                if (Array.isArray(Object(arr))) {
                    jQuery.merge(ret, typeof arr === "string" ? [arr] : arr);
                } else {
                    ret.push(arr);
                }
            }

            return ret;
        },
        inArray: function (elem: any, arr: any, i: any): any {
            return arr == null ? -1 : arr.indexOf(elem, i);
        },
        merge: function (first: any, second: any): any {
            let len = +second.length,
                j = 0,
                i = first.length;

            for (; j < len; j++) {
                first[i++] = second[j];
            }

            first.length = i;

            return first;
        },
        grep: function (elems: any, callback: any, invert: any): any {
            const callbackInverse: any,
                matches: any = [],
                i = 0,
                length = elems.length,
                callbackExpect = !invert;

            for (; i < length; i++) {
                callbackInverse = !callback(elems[i], i);
                if (callbackInverse !== callbackExpect) {
                    matches.push(elems[i]);
                }
            }

            return matches;
        },
        map: function (elems: any, callback: any, arg: any): any {
            let length, value,
                i = 0,
                ret = [];

            if (Array.isArray(elems)) {
                length = elems.length;
                for (; i < length; i++) {
                    value = callback(elems[i], i, arg);

                    if (value != null) {
                        ret.push(value);
                    }
                }
            } else {
                for (i in elems) {
                    value = callback(elems[i], i, arg);

                    if (value != null) {
                        ret.push(value);
                    }
                }
            }

            return [].concat.apply([], ret);
        },
        guid: 1,
        proxy: function (fn: Function, context: any): Function {
            let tmp: any, args: any, proxy: any;

            if (typeof context === "string") {
                tmp = fn[context];
                context = fn;
                fn = tmp;
            }

            if (!jQuery.isFunction(fn)) {
                return undefined;
            }

            args = [].slice.call(arguments, 2);
            proxy = function (): any {
                return fn.apply(context || this, args.concat([].slice.call(arguments)));
            };

            proxy.guid = fn.guid = fn.guid || jQuery.guid++;

            return proxy;
        },
        now: Date.now,
        support: {}
    });

    jQuery.fn = rootjQuery;

    jQuery.extend = jQuery.fn.extend = function (): any {
        let options: any, name: any, src: any, copy: any, copyIsArray: any, clone: any,
            target = arguments[0] || {},
            i = 1,
            length = arguments.length,
            deep = false;

        if (typeof target === "boolean") {
            deep = target;
            target = arguments[i] || {};
            i++;
        }

        if (typeof target !== "object" && !jQuery.isFunction(target)) {
            target = {};
        }

        if (i === length) {
            target = this;
            i--;
        }

        for (; i < length; i++) {
            if ((options = arguments[i]) != null) {
                for (name in options) {
                    src = target[name];
                    copy = options[name];

                    if (target === copy) {
                        continue;
                                   if (deep && copy && (jQuery.isPlainObject(copy) || (copyIsArray = Array.isArray(copy)))) {
                    if (copyIsArray) {
                        copyIsArray = false;
                        clone = src && Array.isArray(src) ? src : [];

                    } else {
                        clone = src && jQuery.isPlainObject(src) ? src : {};
                    }

                    target[name] = jQuery.extend(deep, clone, copy);

                } else if (copy !== undefined) {
                    target[name] = copy;
                }
            }
        }
    }

    return target;
};

jQuery.extend({
    expando: "jQuery" + ("2.1.1" + Math.random()).replace(/\D/g, ""),
    isReady: true,
    error: function (msg: string): never {
        throw new Error(msg);
    },
    noop: function (): void {},
    isFunction: function (obj: any): obj is Function {
        return jQuery.type(obj) === "function";
    },
    isArray: Array.isArray,
    isWindow: function (obj: any): obj is Window {
        return obj != null && obj === obj.window;
    },
    isNumeric: function (obj: any): boolean {
        return !jQuery.isArray(obj) && obj - parseFloat(obj) >= 0;
    },
    isPlainObject: function (obj: any): obj is PlainObject {
        if (jQuery.type(obj) !== "object" || obj.nodeType || jQuery.isWindow(obj)) {
            return false;
        }
        if (obj.constructor && !Object.prototype.hasOwnProperty.call(obj.constructor.prototype, "isPrototypeOf")) {
            return false;
        }
        return true;
    },
    isEmptyObject: function (obj: any): boolean {
        for (const name in obj) {
            return false;
        }
        return true;
    },
    type: function (obj: any): string {
        if (obj == null) {
            return obj + "";
        }
        return typeof obj === "object" || typeof obj === "function"
            ? Object.prototype.toString.call(obj).slice(8, -1).toLowerCase()
            : typeof obj;
    },
    globalEval: function (code: string): void {
        const script: HTMLScriptElement = document.createElement("script");
        script.text = code;
        document.head.appendChild(script).parentNode!.removeChild(script);
    },
    camelCase: function (str: string): string {
        return str.replace(/^-ms-/, "ms-").replace(/-([\da-z])/gi, function (all, letter) {
            return letter.toUpperCase();
        });
    },
    nodeName: function (elem: Node, name: string): boolean {
        return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();
    },
    each: function (obj: any, callback: any): any {
        let length, i = 0;

        if (Array.isArray(obj)) {
            length = obj.length;
            for (; i < length; i++) {
                if (callback.call(obj[i], i, obj[i]) === false) {
                    break;
                }
            }
        } else {
            for (i in obj) {
                if (callback.call(obj[i], i, obj[i]) === false) {
                    break;
                }
            }
        }

        return obj;
    },
    trim: function (text: string): string {
        return text == null ? "" : (text + "").replace(/^\s+|\s+$/g, "");
    },
    makeArray: function (arr: any, results: any): any {
        const ret = results || [];

        if (arr != null) {
            if (Array.isArray(Object(arr))) {
                jQuery.merge(ret, typeof arr === "string" ? [arr] : arr);
            } else {
                ret.push(arr);
            }
        }

        return ret;
    },
    inArray: function (elem: any, arr: any, i: any): any {
        return arr == null ? -1 : arr.indexOf(elem, i);
    },
    merge: function (first: any, second: any): any {
        let len = +second.length,
            j = 0,
            i = first.length;

        for (; j < len; j++) {
            first[i++] = second[j];
        }

        first.length = i;

        return first;
    },
    grep: function (elems: any, callback: any, invert: any): any {
        const callbackInverse: any,
            matches: any = [],
            i = 0,
            length = elems.length,
            callbackExpect = !invert;

        for (; i < length; i++) {
            callbackInverse = !callback(elems[i], i);
            if (callbackInverse !== callbackExpect) {
                matches.push(elems[i]);
            }
        }

        return matches;
    },
    map: function (elems: any, callback: any, arg: any): any {
        let length, value,
            i = 0,
            ret = [];

        if (Array.isArray(elems)) {
            length = elems.length;
            for (; i < length; i++) {
                value = callback(elems[i], i, arg);

                if (value != null) {
                    ret.push(value);
                }
            }
        } else {
            for (i in elems) {
                value = callback(elems[i], i, arg);

                if (value != null) {
                    ret.push(value);
                }
            }
        }

        return [].concat.apply([], ret);
    },
    guid: 1,
    proxy: function (fn: Function, context: any): Function {
        let tmp: any, args: any, proxy: any;

        if (typeof context === "string") {
            tmp = fn[context];
            context = fn;
            fn = tmp;
        }

        if (!jQuery.isFunction(fn)) {
            return undefined;
        }

        args = [].slice.call(arguments, 2);
        proxy = function (): any {
            return fn.apply(context || this, args.concat([].slice.call(arguments)));
        };

        proxy.guid = fn.guid = fn.guid || jQuery.guid++;

        return proxy;
    },
    now: Date.now,
    support: {}
});

jQuery.fn = rootjQuery;

jQuery.extend = jQuery.fn.extend = function (): any {
    let options: any, name: any, src: any, copy: any, copyIsArray: any, clone: any,
        target = arguments[0] || {},
        i = 1,
        length = arguments.length,
        deep = false;

    if (typeof target === "boolean") {
        deep = target;
        target = arguments[i] || {};
        i++;
    }

    if (typeof target !== "object" && !jQuery.isFunction(target)) {
        target = {};
    }

    if (i === length) {
        target = this;
        i--;
    }

    for (; i < length; i++) {
        if ((options = arguments[i]) != null) {
            for (name in options) {
                src = target[name];
                copy = options[name];

                if (target === copy) {
                    continue;
                }

                if (deep && copy && (jQuery.isPlainObject(copy) || (copyIsArray = Array.isArray(copy)))) {
                    if (copyIsArray) {
                        copyIsArray = false;
                        clone = src && Array.isArray(src) ? src : [];

                    } else {
                        clone = src && jQuery.isPlainObject(src) ? src : {};
                    }

                    target[name] = jQuery.extend(deep, clone, copy);

                } else if (copy !== undefined) {
                    target[name] = copy;
                }
            }
        }
    }

    return target;
};

jQuery.extend({
    expando: "jQuery" + ("2.1.1" + Math.random()).replace(/\D/g, ""),
    isReady: true,
    error: function (msg: string): never {
        throw new Error(msg);
    },
    noop: function (): void {},
    isFunction: function (obj: any): obj is Function {
        return jQuery.type(obj) === "function";
    },
    isArray: Array.isArray,
    isWindow: function (obj: any): obj is Window {
        return obj != null && obj === obj.window;
    },
    isNumeric: function (obj: any): boolean {
        return !jQuery.isArray(obj) && obj - parseFloat(obj) >= 0;
    },
    isPlainObject: function (obj: any): obj is PlainObject {
        if (jQuery.type(obj) !== "object" || obj.nodeType || jQuery.isWindow(obj)) {
            return false;
        }
        if (obj.constructor && !Object.prototype.hasOwnProperty.call(obj.constructor.prototype, "isPrototypeOf")) {
            return false;
        }
        return true;
    },
    isEmptyObject: function (obj: any): boolean {
        for (const name in obj) {
            return false;
        }
        return true;
    },
    type: function (obj: any): string {
        if (obj == null) {
            return obj + "";
        }
        return typeof obj === "object" || typeof obj === "function"
            ? Object.prototype.toString.call(obj).slice(8, -1).toLowerCase()
            : typeof obj;
    },
    globalEval: function (code: string): void {
        const script: HTMLScriptElement = document.createElement("script");
        script.text = code;
        document.head.appendChild(script).parentNode!.removeChild(script);
    },
    camelCase: function (str: string): string {
        return str.replace(/^-ms-/, "ms-").replace(/-([\da-z])/gi, function (all, letter) {
            return letter.toUpperCase();
        });
    },
    nodeName: function (elem: Node, name: string): boolean {
        return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();
    },
    each: function (obj: any, callback: any): any {
        let length, i = 0;

        if (Array.isArray(obj)) {
            length = obj.length;
            for (; i < length; i++) {
                if (callback.call(obj[i], i, obj[i]) === false) {
                    break;
                }
            }
        } else {
            for (i in obj) {
                if (callback.call(obj[i], i, obj[i]) === false) {
                    break;
                }
            }
        }

        return obj;
    },
    trim: function (text: string): string {
        return text == null ? "" : (text + "").replace(/^\s+|\s+$/g, "");
    },
    makeArray: function (arr: any, results: any): any {
        const ret = results || [];

        if (arr != null) {
            if (Array.isArray(Object(arr))) {
                jQuery.merge(ret, typeof arr === "string" ? [arr] : arr);
            } else {
                ret.push(arr);
            }
        }

        return ret;
    },
    inArray: function (elem: any, arr: any, i: any): any {
        return arr == null ? -1 : arr.indexOf(elem, i);
    },
    merge: function (first: any, second: any): any {
        let len = +second.length,
            j = 0,
            i = first.length;

        for (; j < len; j++) {
            first[i++] = second[j];
        }

        first.length = i;

        return first;
    },
    grep: function (elems: any, callback: any, invert: any): any {
        const callbackInverse: any,
            matches: any = [],
            i = 0,
            length = elems.length,
            callbackExpect = !invert;

        for (; i < length; i++) {
            callbackInverse = !callback(elems[i], i);
            if (callbackInverse !== callbackExpect) {
                matches.push(elems[i]);
            }
        }

        return matches;
    },
    map: function (elems: any, callback: any, arg: any): any {
        let length, value,
            i = 0,
            ret = [];

        if (Array.isArray(elems)) {
            length = elems.length;
            for (; i < length; i++) {
                value = callback(elems[i], i, arg);

                if (value != null) {
                    ret.push(value);
                }
            }
        } else {
            for (i in elems) {
                value = callback(elems[i], i, arg);

                if (value != null) {
                    ret.push(value);
                }
            }
        }

        return [].concat.apply([], ret);
    },
    guid: 1,
    proxy: function (fn: Function, context: any): Function {
        let tmp: any, args: any, proxy: any;

        if (typeof context === "string") {
            tmp = fn[context];
            context = fn;
            fn = tmp;
        }

        if (!jQuery.isFunction(fn)) {
            return undefined;
        }

        args = [].slice.call(arguments, 2);
        proxy = function (): any {
            return fn.apply(context || this, args.concat([].slice.call(arguments)));
        };

        proxy.guid = fn.guid = fn.guid || jQuery.guid++;

        return proxy;
    },
    now: Date.now,
    support: {}
});

jQuery.fn = rootjQuery;

jQuery.extend = jQuery.fn.extend = function (): any {
    let options: any, name: any, src: any, copy: any, copyIsArray: any, clone: any,
        target = arguments[0] || {},
        i = 1,
        length = arguments.length,
        deep = false;

    if (typeof target === "boolean") {
        deep = target;
        target = arguments[i] || {};
        i++;
    }

    if (typeof target !== "object" && !jQuery.isFunction(target)) {
        target = {};
    }

    if (i === length) {
        target = this;
        i--;
    }

    for (; i < length; i++) {
        if ((options = arguments[i]) != null) {
            for (name in options) {
                src = target[name];
                copy = options[name];

                if (target === copy) {
                    continue;
                }

                if (deep && copy && (jQuery.isPlainObject(copy) || (copyIsArray = Array.isArray(copy)))) {
                    if (copyIsArray) {
                        copyIsArray = false;
                        clone = src && Array.isArray(src) ? src : [];

                    } else {
                        clone = src && jQuery.isPlainObject(src) ? src : {};
                    }

                    target[name] = jQuery.extend(deep, clone, copy);

                } else if (copy !== undefined) {
                    target[name] = copy;
                }
            }
        }
    }

    return target;
};

jQuery.extend({
    expando: "jQuery" + ("2.1.1" + Math.random()).replace(/\D/g, ""),
    isReady: true,
    error: function (msg: string): never {
        throw new Error(msg);
    },
    noop: function (): void {},
    isFunction: function (obj: any): obj is Function {
        return jQuery.type(obj) === "function";
    },
    isArray: Array.isArray,
    isWindow: function (obj: any): obj is Window {
        return obj != null && obj === obj.window;
    },
    isNumeric: function (obj: any): boolean {
        return !jQuery.isArray(obj) && obj - parseFloat(obj) >= 0;
    },
    isPlainObject: function (obj: any): obj is PlainObject {
        if (jQuery.type(obj) !== "object" || obj.nodeType || jQuery.isWindow(obj)) {
            return false;
        }
        if (obj.constructor && !Object.prototype.hasOwnProperty.call(obj.constructor.prototype, "isPrototypeOf")) {
            return false;
        }
        return true;
    },
    isEmptyObject: function (obj: any): boolean {
        for (const name in obj) {
            return false;
        }
        return true;
    },
    type: function (obj: any): string {
        if (obj == null) {
            return obj + "";
        }
        return typeof obj === "object" || typeof obj === "function"
            ? Object.prototype.toString.call(obj).slice(8, -1).toLowerCase()
            : typeof obj;
    },
    globalEval: function (code: string): void {
        const script: HTMLScriptElement = document.createElement("script");
        script.text = code;
        document.head.appendChild(script).parentNode!.removeChild(script);
    },
    camelCase: function (str: string): string {
        return str.replace(/^-ms-/, "ms-").replace(/-([\da-z])/gi, function (all, letter) {
            return letter.toUpperCase();
        });
    },
    nodeName: function (elem: Node, name: string): boolean {
        return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();
    },
    each: function (obj: any, callback: any): any {
        let length, i = 0;

        if (Array.isArray(obj)) {
            length = obj.length;
            for (; i < length; i++) {
                if (callback.call(obj[i], i, obj[i]) === false) {
                    break;
                }
            }
        } else {
            for (i in obj) {
                if (callback.call(obj[i], i, obj[i]) === false) {
                    break;
                }
            }
        }

        return obj;
    },
    trim: function (text: string): string {
        return text == null ? "" : (text + "").replace(/^\s+|\s+$/g, "");
    },
    makeArray: function (arr: any, results: any): any {
        const ret = results || [];

        if (arr != null) {
            if (Array.isArray(Object(arr))) {
                jQuery.merge(ret, typeof arr === "string" ? [arr] : arr);
            } else {
                ret.push(arr);
            }
        }

        return ret;
    },
    inArray: function (elem: any, arr: any, i: any): any {
        return arr == null ? -1 : arr.indexOf(elem, i);
    },
    merge: function (first: any, second: any): any {
        let len = +second.length,
            j = 0,
            i = first.length;

        for (; j < len; j++) {
            first[i++] = second[j];
        }

        first.length = i;

        return first;
    },
    grep: function (elems: any, callback: any, invert: any): any {
        const callbackInverse: any,
            matches: any = [],
            i = 0,
            length = elems.length,
            callbackExpect = !invert;

        for (; i < length; i++) {
            callbackInverse = !callback(elems[i], i);
            if (callbackInverse !== callbackExpect) {
                matches.push(elems[i]);
                       }
        }

        return matches;
    },
    map: function (elems: any, callback: any, arg: any): any {
        let length, value,
            i = 0,
            ret = [];

        if (Array.isArray(elems)) {
            length = elems.length;
            for (; i < length; i++) {
                value = callback(elems[i], i, arg);

                if (value != null) {
                    ret.push(value);
                }
            }
        } else {
            for (i in elems) {
                value = callback(elems[i], i, arg);

                if (value != null) {
                    ret.push(value);
                }
            }
        }

        return [].concat.apply([], ret);
    },
    guid: 1,
    proxy: function (fn: Function, context: any): Function {
        let tmp: any, args: any, proxy: any;

        if (typeof context === "string") {
            tmp = fn[context];
            context = fn;
            fn = tmp;
        }

        if (!jQuery.isFunction(fn)) {
            return undefined;
        }

        args = [].slice.call(arguments, 2);
        proxy = function (): any {
            return fn.apply(context || this, args.concat([].slice.call(arguments)));
        };

        proxy.guid = fn.guid = fn.guid || jQuery.guid++;

        return proxy;
    },
    now: Date.now,
    support: {}
});

jQuery.fn = rootjQuery;

jQuery.extend = jQuery.fn.extend = function (): any {
    let options: any, name: any, src: any, copy: any, copyIsArray: any, clone: any,
        target = arguments[0] || {},
        i = 1,
        length = arguments.length,
        deep = false;

    if (typeof target === "boolean") {
        deep = target;
        target = arguments[i] || {};
        i++;
    }

    if (typeof target !== "object" && !jQuery.isFunction(target)) {
        target = {};
    }

    if (i === length) {
        target = this;
        i--;
    }

    for (; i < length; i++) {
        if ((options = arguments[i]) != null) {
            for (name in options) {
                src = target[name];
                copy = options[name];

                if (target === copy) {
                    continue;
                }

                if (deep && copy && (jQuery.isPlainObject(copy) || (copyIsArray = Array.isArray(copy)))) {
                    if (copyIsArray) {
                        copyIsArray = false;
                        clone = src && Array.isArray(src) ? src : [];

                    } else {
                        clone = src && jQuery.isPlainObject(src) ? src : {};
                    }

                    target[name] = jQuery.extend(deep, clone, copy);

                } else if (copy !== undefined) {
                    target[name] = copy;
                }
            }
        }
    }

    return target;
};

jQuery.extend({
    expando: "jQuery" + ("2.1.1" + Math.random()).replace(/\D/g, ""),
    isReady: true,
    error: function (msg: string): never {
        throw new Error(msg);
    },
    noop: function (): void {},
    isFunction: function (obj: any): obj is Function {
        return jQuery.type(obj) === "function";
    },
    isArray: Array.isArray,
    isWindow: function (obj: any): obj is Window {
        return obj != null && obj === obj.window;
    },
    isNumeric: function (obj: any): boolean {
        return !jQuery.isArray(obj) && obj - parseFloat(obj) >= 0;
    },
    isPlainObject: function (obj: any): obj is PlainObject {
        if (jQuery.type(obj) !== "object" || obj.nodeType || jQuery.isWindow(obj)) {
            return false;
        }
        if (obj.constructor && !Object.prototype.hasOwnProperty.call(obj.constructor.prototype, "isPrototypeOf")) {
            return false;
        }
        return true;
    },
    isEmptyObject: function (obj: any): boolean {
        for (const name in obj) {
            return false;
        }
        return true;
    },
    type: function (obj: any): string {
        if (obj == null) {
            return obj + "";
        }
        return typeof obj === "object" || typeof obj === "function"
            ? Object.prototype.toString.call(obj).slice(8, -1).toLowerCase()
            : typeof obj;
    },
    globalEval: function (code: string): void {
        const script: HTMLScriptElement = document.createElement("script");
        script.text = code;
        document.head.appendChild(script).parentNode!.removeChild(script);
    },
    camelCase: function (str: string): string {
        return str.replace(/^-ms-/, "ms-").replace(/-([\da-z])/gi, function (all, letter) {
            return letter.toUpperCase();
        });
    },
    nodeName: function (elem: Node, name: string): boolean {
        return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();
    },
    each: function (obj: any, callback: any): any {
        let length, i = 0;

        if (Array.isArray(obj)) {
            length = obj.length;
            for (; i < length; i++) {
                if (callback.call(obj[i], i, obj[i]) === false) {
                    break;
                }
            }
        } else {
            for (i in obj) {
                if (callback.call(obj[i], i, obj[i]) === false) {
                    break;
                }
            }
        }

        return obj;
    },
    trim: function (text: string): string {
        return text == null ? "" : (text + "").replace(/^\s+|\s+$/g, "");
    },
    makeArray: function (arr: any, results: any): any {
        const ret = results || [];

        if (arr != null) {
            if (Array.isArray(Object(arr))) {
                jQuery.merge(ret, typeof arr === "string" ? [arr] : arr);
            } else {
                ret.push(arr);
            }
        }

        return ret;
    },
    inArray: function (elem: any, arr: any, i: any): any {
        return arr == null ? -1 : arr.indexOf(elem, i);
    },
    merge: function (first: any, second: any): any {
        let len = +second.length,
            j = 0,
            i = first.length;

        for (; j < len; j++) {
            first[i++] = second[j];
        }

        first.length = i;

        return first;
    },
    grep: function (elems: any, callback: any, invert: any): any {
        const callbackInverse: any,
            matches: any = [],
            i = 0,
            length = elems.length,
            callbackExpect = !invert;

        for (; i < length; i++) {
            callbackInverse = !callback(elems[i], i);
            if (callbackInverse !== callbackExpect) {
                matches.push(elems[i]);
            }
        }

        return matches;
    },
    map: function (elems: any, callback: any, arg: any): any {
        let length, value,
            i = 0,
            ret = [];

        if (Array.isArray(elems)) {
            length = elems.length;
            for (; i < length; i++) {
                value = callback(elems[i], i, arg);

                if (value != null) {
                    ret.push(value);
                }
            }
        } else {
            for (i in elems) {
                value = callback(elems[i], i, arg);

                if (value != null) {
                    ret.push(value);
                }
            }
        }

        return [].concat.apply([], ret);
    },
    guid: 1,
    proxy: function (fn: Function, context: any): Function {
        let tmp: any, args: any, proxy: any;

        if (typeof context === "string") {
            tmp = fn[context];
            context = fn;
            fn = tmp;
        }

        if (!jQuery.isFunction(fn)) {
            return undefined;
        }

        args = [].slice.call(arguments, 2);
        proxy = function (): any {
            return fn.apply(context || this, args.concat([].slice.call(arguments)));
        };

        proxy.guid = fn.guid = fn.guid || jQuery.guid++;

        return proxy;
    },
    now: Date.now,
    support: {}
});

jQuery.fn = rootjQuery;

jQuery.extend = jQuery.fn.extend = function (): any {
    let options: any, name: any, src: any, copy: any, copyIsArray: any, clone: any,
        target = arguments[0] || {},
        i = 1,
        length = arguments.length,
        deep = false;

    if (typeof target === "boolean") {
        deep = target;
        target = arguments[i] || {};
        i++;
    }

    if (typeof target !== "object" && !jQuery.isFunction(target)) {
        target = {};
    }

    if (i === length) {
        target = this;
        i--;
    }

    for (; i < length; i++) {
        if ((options = arguments[i]) != null) {
            for (name in options) {
                src = target[name];
                copy = options[name];

                if (target === copy) {
                    continue;
                }

                if (deep && copy && (jQuery.isPlainObject(copy) || (copyIsArray = Array.isArray(copy)))) {
                    if (copyIsArray) {
                        copyIsArray = false;
                        clone = src && Array.isArray(src) ? src : [];

                    } else {
                        clone = src && jQuery.isPlainObject(src) ? src : {};
                    }

                    target[name] = jQuery.extend(deep, clone, copy);

                } else if (copy !== undefined) {
                    target[name] = copy;
                }
            }
        }
    }

    return target;
};

jQuery.extend({
    expando: "jQuery" + ("2.1.1" + Math.random()).replace(/\D/g, ""),
    isReady: true,
    error: function (msg: string): never {
        throw new Error(msg);
    },
    noop: function (): void {},
    isFunction: function (obj: any): obj is Function {
        return jQuery.type(obj) === "function";
    },
    isArray: Array.isArray,
    isWindow: function (obj: any): obj is Window {
        return obj != null && obj === obj.window;
    },
    isNumeric: function (obj: any): boolean {
        return !jQuery.isArray(obj) && obj - parseFloat(obj) >= 0;
    },
    isPlainObject: function (obj: any): obj is PlainObject {
        if (jQuery.type(obj) !== "object" || obj.nodeType || jQuery.isWindow(obj)) {
            return false;
        }
        if (obj.constructor && !Object.prototype.hasOwnProperty.call(obj.constructor.prototype, "isPrototypeOf")) {
            return false;
        }
        return true;
    },
    isEmptyObject: function (obj: any): boolean {
        for (const name in obj) {
            return false;
        }
        return true;
    },
    type: function (obj: any): string {
        if (obj == null) {
            return obj + "";
        }
        return typeof obj === "object" || typeof obj === "function"
            ? Object.prototype.toString.call(obj).slice(8, -1).toLowerCase()
            : typeof obj;
    },
    globalEval: function (code: string): void {
        const script: HTMLScriptElement = document.createElement("script");
        script.text = code;
        document.head.appendChild(script).parentNode!.removeChild(script);
    },
    camelCase: function (str: string): string {
        return str.replace(/^-ms-/, "ms-").replace(/-([\da-z])/gi, function (all, letter) {
            return letter.toUpperCase();
        });
    },
    nodeName: function (elem: Node, name: string): boolean {
        return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();
    },
    each: function (obj: any, callback: any): any {
        let length, i = 0;

        if (Array.isArray(obj)) {
            length = obj.length;
            for (; i < length; i++) {
                if (callback.call(obj[i], i, obj[i]) === false) {
                    break;
                }
            }
        } else {
            for (i in obj) {
                if (callback.call(obj[i], i, obj[i]) === false) {
                    break;
                }
            }
        }

        return obj;
    },
    trim: function (text: string): string {
        return text == null ? "" : (text + "").replace(/^\s+|\s+$/g, "");
    },
    makeArray: function (arr: any, results: any): any {
        const ret = results || [];

        if (arr != null) {
            if (Array.isArray(Object(arr))) {
                jQuery.merge(ret, typeof arr === "string" ? [arr] : arr);
            } else {
                ret.push(arr);
            }
        }

        return ret;
    },
    inArray: function (elem: any, arr: any, i: any): any {
        return arr == null ? -1 : arr.indexOf(elem, i);
    },
    merge: function (first: any, second: any): any {
        let len = +second.length,
            j = 0,
            i = first.length;

        for (; j < len; j++) {
            first[i++] = second[j];
        }

        first.length = i;

        return first;
    },
    grep: function (elems: any, callback: any, invert: any): any {
        const callbackInverse: any,
            matches: any = [],
            i = 0,
            length = elems.length,
            callbackExpect = !invert;

        for (; i < length; i++) {
            callbackInverse = !callback(elems[i], i);
            if (callbackInverse !== callbackExpect) {
                matches.push(elems[i]);
            }
        }

        return matches;
    },
    map: function (elems: any, callback: any, arg: any): any {
        let length, value,
            i = 0,
            ret = [];

        if (Array.isArray(elems)) {
            length = elems.length;
            for (; i < length; i++) {
                value = callback(elems[i], i, arg);

                if (value != null) {
                    ret.push(value);
                }
            }
        } else {
            for (i in elems) {
                value = callback(elems[i], i, arg);

                if (value != null) {
                    ret.push(value);
                }
            }
        }

        return [].concat.apply([], ret);
    },
    guid: 1,
    proxy: function (fn: Function, context: any): Function {
        let tmp: any, args: any, proxy: any;

        if (typeof context === "string") {
            tmp = fn[context];
            context = fn;
            fn = tmp;
        }

        if (!jQuery.isFunction(fn)) {
            return undefined;
        }

        args = [].slice.call(arguments, 2);
        proxy = function (): any {
            return fn.apply(context || this, args.concat([].slice.call(arguments)));
        };

        proxy.guid = fn.guid = fn.guid || jQuery.guid++;

        return proxy;
    },
    now: Date.now,
    support: {}
});

jQuery.fn = rootjQuery;

jQuery.extend = jQuery.fn.extend = function (): any {
    let options: any, name: any, src: any, copy: any, copyIsArray: any, clone: any,
        target = arguments[0] || {},
        i = 1,
        length = arguments.length,
        deep = false;

    if (typeof target === "boolean") {
        deep = target;
        target = arguments[i] || {};
        i++;
    }

    if (typeof target !== "object" && !jQuery.isFunction(target)) {
        target = {};
    }

    if (i === length) {
        target = this;
        i--;
    }

    for (; i < length; i++) {
        if ((options = arguments[i]) != null) {
            for (name in options) {
                src = target[name];
                copy = options[name];

                if (target === copy) {
                    continue;
                }

                if (deep && copy && (jQuery.isPlainObject(copy) || (copyIsArray = Array.isArray(copy)))) {
                    if (copyIsArray) {
                        copyIsArray = false;
                        clone = src && Array.isArray(src) ? src : [];

                    } else {
                        clone = src && jQuery.isPlainObject(src) ? src : {};
                    }

                    target[name] = jQuery.extend(deep, clone, copy);

                } else if (copy !== undefined) {
                    target[name] = copy;
                }
            }
        }
    }

    return target;
};

jQuery.extend({
    expando: "jQuery" + ("2.1.1" + Math.random()).replace(/\D/g, ""),
    isReady: true,
    error: function (msg: string): never {
        throw new Error(msg);
    },
    noop: function (): void {},
    isFunction: function (obj: any): obj is Function {
        return jQuery.type(obj) === "function";
    },
    isArray: Array.isArray,
    isWindow: function (obj: any): obj is Window {
        return obj != null && obj === obj.window;
    },
    isNumeric: function (obj: any): boolean {
        return !jQuery.isArray(obj) && obj - parseFloat(obj) >= 0;
    },
    isPlainObject: function (obj: any): obj is PlainObject {
        if (jQuery.type(obj) !== "object" || obj.nodeType || jQuery.isWindow(obj)) {
            return false;
        }
        if (obj.constructor && !Object.prototype.hasOwnProperty.call(obj.constructor.prototype, "isPrototypeOf")) {
            return false;
        }
        return true;
    },
    isEmptyObject: function (obj: any): boolean {
        for (const name in obj) {
            return false;
        }
        return true;
    },
    type: function (obj: any): string {
        if (obj == null) {
            return obj + "";
        }
        return typeof obj === "object" || typeof obj === "function"
            ? Object.prototype.toString.call(obj).slice(8, -1).toLowerCase()
            : typeof obj;
    },
    globalEval: function (code: string): void {
        const script: HTMLScriptElement = document.createElement("script");
        script.text = code;
        document.head.appendChild(script).parentNode!.removeChild(script);
    },
    camelCase: function (str: string): string {
        return str.replace(/^-ms-/, "ms-").replace(/-([\da-z])/gi, function (all, letter) {
            return letter.toUpperCase();
        });
    },
    nodeName: function (elem: Node, name: string): boolean {
        return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();
    },
    each: function (obj: any, callback: any): any {
        let length, i = 0;

        if (Array.isArray(obj)) {
            length = obj.length;
            for (; i < length; i++) {
                if (callback.call(obj[i], i, obj[i]) === false) {
                    break;
                }
            }
        } else {
            for (i in obj) {
                if (callback.call(obj[i], i, obj[i]) === false) {
                    break;
                }
            }
        }

        return obj;
    },
    trim: function (text: string): string {
        return text == null ? "" : (text + "").replace(/^\s+|\s+$/g, "");
    },
    makeArray: function (arr: any, results: any): any {
        const ret = results || [];

        if (arr != null) {
            if (Array.isArray(Object(arr))) {
                jQuery.merge(ret, typeof arr === "string" ? [arr] : arr);
            } else {
                ret.push(arr);
            }
        }

        return ret;
    },
    inArray: function (elem: any, arr: any, i: any): any {
        return arr == null ? -1 : arr.indexOf(elem, i);
    },
    merge: function (first: any, second: any): any {
        let len = +second.length,
            j = 0,
            i = first.length;

        for (; j < len; j++) {
            first[i++] = second[j];
        }

        first.length = i;

        return first;
    },
    grep: function (elems: any, callback: any, invert: any): any {
        const callbackInverse: any,
            matches: any = [],
            i = 0,
            length = elems.length,
            callbackExpect = !invert;

        for (; i < length; i++) {
            callbackInverse = !callback(elems[i], i);
            if (callbackInverse !== callbackExpect) {
                matches.push(elems[i]);
            }
        }

        return matches;
    },
    map: function (elems: any, callback: any, arg: any): any {
        let length, value,
            i = 0,
            ret = [];

        if (Array.isArray(elems)) {
            length = elems.length;
            for (; i < length; i++) {
                value = callback(elems[i], i, arg);

                if (value != null) {
                    ret.push(value);
                }
            }
        } else {
            for (i in elems) {
                value = callback(elems[i], i, arg);

                if (value != null) {
                    ret.push(value);
                }
            }
        }

        return [].concat.apply([], ret);
    },
    guid: 1,
    proxy: function (fn: Function, context: any): Function {
        let tmp: any, args: any, proxy: any;

        if (typeof context === "string") {
            tmp = fn[context];
            context = fn;
            fn = tmp;
        }

        if (!jQuery.isFunction(fn)) {
            return undefined;
        }

        args = [].slice.call(arguments, 2);
        proxy = function (): any {
            return fn.apply(context || this, args.concat([].slice.call(arguments)));
        };

        proxy.guid = fn.guid = fn.guid || jQuery.guid++;

        return proxy;
    },
    now: Date.now,
    support: {}
});

jQuery.fn = rootjQuery;

jQuery.extend = jQuery.fn.extend = function (): any {
    let options: any, name: any, src: any, copy: any, copyIsArray: any, clone: any,
        target = arguments[0] || {},
        i = 1,
        length = arguments.length,
        deep = false;

    if (typeof target === "boolean") {
        deep = target;
        target = arguments[i] || {};
        i++;
    }

    if (typeof target !== "object" && !jQuery.isFunction(target)) {
        target = {};
    }

    if (i === length) {
        target = this;
        i--;
    }

    for (; i < length; i++) {
        if ((options = arguments[i]) != null) {
            for (name in options) {
                src = target[name];
                copy = options[name];

                if (target === copy) {
                    continue;
                }

                if (deep && copy && (jQuery.isPlainObject(copy) || (copyIsArray = Array.isArray(copy)))) {
                    if (copyIsArray) {
                        copyIsArray = false;
                        clone = src && Array.isArray(src) ? src : [];

                    } else {
                        clone = src && jQuery.isPlainObject(src) ? src : {};
                    }

                    target[name] = jQuery.extend(deep, clone, copy);

                } else if (copy !== undefined) {
                    target[name] = copy;
                }
            }
        }
    }

    return target;
};

jQuery.extend({
    expando: "jQuery" + ("2.1.1" + Math.random()).replace(/\D/g, ""),
    isReady: true,
    error: function (msg: string): never {
        throw new Error(msg);
    },
    noop: function (): void {},
    isFunction: function (obj: any): obj is Function {
        return jQuery.type(obj) === "function";
    },
    isArray: Array.isArray,
    isWindow: function (obj: any): obj is Window {
        return obj != null && obj === obj.window;
    },
    isNumeric: function (obj: any): boolean {
        return !jQuery.isArray(obj) && obj - parseFloat(obj) >= 0;
    },
    isPlainObject: function (obj: any): obj is PlainObject {
        if (jQuery.type(obj) !== "object" || obj.nodeType || jQuery.isWindow(obj)) {
            return false;
        }
        if (obj.constructor && !Object.prototype.hasOwnProperty.call(obj.constructor.prototype, "isPrototypeOf")) {
            return false;
        }
        return true;
    },
    isEmptyObject: function (obj: any): boolean {
        for (const name in obj) {
            return false;
        }
        return true;
    },
    type: function (obj: any): string {
        if (obj == null) {
            return obj + "";
        }
        return typeof obj === "object" || typeof obj === "function"
            ? Object.prototype.toString.call(obj).slice(8, -1).toLowerCase()
            : typeof obj;
    },
    globalEval: function (code: string): void {
        const script: HTMLScriptElement = document.createElement("script");
        script.text = code;
        document.head.appendChild(script).parentNode!.removeChild(script);
    },
    camelCase: function (str: string): string {
        return str.replace(/^-ms-/, "ms-").replace(/-([\da-z])/gi, function (all, letter) {
            return letter.toUpperCase();
        });
    },
    nodeName: function (elem: Node, name: string): boolean {
        return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();
    },
    each: function (obj: any, callback: any): any {
        let length, i = 0;

        if (Array.isArray(obj)) {
            length = obj.length;
            for (; i < length; i++) {
                if (callback.call(obj[i], i, obj[i]) === false) {
                    break;
                }
            }
        } else {
            for (i in obj) {
                if (callback.call(obj[i], i, obj[i]) === false) {
                    break;
                }
            }
        }

        return obj;
    },
    trim: function (text: string): string {
        return text == null ? "" : (text + "").replace(/^\s+|\s+$/g, "");
    },
    makeArray: function (arr: any, results: any): any {
        const ret = results || [];

        if (arr != null) {
            if (Array.isArray(Object(arr))) {
                jQuery.merge(ret, typeof arr === "string" ? [arr] : arr);
            } else {
                ret.push(arr);
            }
        }

        return ret;
    },
    inArray: function (elem: any, arr: any, i: any): any {
        return arr == null ? -1 : arr.indexOf(elem, i);
    },
    merge: function (first: any, second: any): any {
        let len = +second.length,
            j = 0,
            i = first.length;

        for (; j < len; j++) {
            first[i++] = second[j];
        }

        first.length = i;

        return first;
    },
    grep: function (elems: any, callback: any, invert: any): any {
        const callbackInverse: any,
            matches: any = [],
            i = 0,
            length = elems.length,
            callbackExpect = !invert;

        for (; i < length; i++) {
            callbackInverse = !callback(elems[i], i);
            if (callbackInverse !== callbackExpect) {
                matches.push(elems[i]);
            }
        }

        return matches;
    },
    map: function (elems: any, callback: any, arg: any): any {
        let length, value,
            i = 0,
            ret = [];

        if (Array.isArray(elems)) {
            length = elems.length;
            for (; i < length; i++) {
                value = callback(elems[i], i, arg);

                if (value != null) {
                    ret.push(value);
                }
            }
        } else {
            for (i in elems) {
                value = callback(elems[i], i, arg);

                if (value != null) {
                    ret.push(value);
                }
            }
        }

        return [].concat.apply([], ret);
    },
    guid: 1,
    proxy: function (fn: Function, context: any): Function {
        let tmp: any, args: any, proxy: any;

        if (typeof context === "string") {
            tmp = fn[context];
            context = fn;
            fn = tmp;
        }

        if (!jQuery.isFunction(fn)) {
            return undefined;
        }

        args = [].slice.call(arguments, 2);
        proxy = function (): any {
            return fn.apply(context || this, args.concat([].slice.call(arguments)));
        };

        proxy.guid = fn.guid = fn.guid || jQuery.guid++;

        return proxy;
    },
    now: Date.now,
    support: {}
});

jQuery.fn = rootjQuery;

jQuery.extend = jQuery.fn.extend = function (): any {
    let options: any, name: any, src: any, copy: any, copyIsArray: any, clone: any,
        target = arguments[0] || {},
        i = 1,
        length = arguments.length,
        deep = false;

    if (typeof target === "boolean") {
        deep = target;
        target = arguments[i] || {};
        i++;
    }

    if (typeof target !== "object" && !jQuery.isFunction(target)) {
        target = {};
    }

    if (i === length) {
        target = this;
        i--;
    }

    for (; i < length; i++) {
        if ((options = arguments[i]) != null) {
            for (name in options) {
                src = target[name];
                copy = options[name];

                if (target === copy) {
                    continue;
                }

                if (deep && copy && (jQuery.isPlainObject(copy) || (copyIsArray = Array.isArray(copy)))) {
                    if (copyIsArray) {
                        copyIsArray = false;
                        clone = src && Array.isArray(src) ? src : [];

                    } else {
                        clone = src && jQuery.isPlainObject(src) ? src : {};
                    }

                    target[name] = jQuery.extend(deep, clone, copy);

                } else if (copy !== undefined) {
                    target[name] = copy;
                }
            }
        }
    }

    return target;
};

jQuery.extend({
    expando: "jQuery" + ("2.1.1" + Math.random()).replace(/\D/g, ""),
    isReady: true,
    error: function (msg: string): never {
        throw new Error(msg);
    },
    noop: function (): void {},
    isFunction: function (obj: any): obj is Function {
        return jQuery.type(obj) === "function";
    },
    isArray: Array.isArray,
    isWindow: function (obj: any): obj is Window {
        return obj != null && obj === obj.window;
    },
    isNumeric: function (obj: any): boolean {
        return !jQuery.isArray(obj) && obj - parseFloat(obj) >= 0;
    },
    isPlainObject: function (obj: any): obj is PlainObject {
        if (jQuery.type(obj) !== "object" || obj.nodeType || jQuery.isWindow(obj)) {
            return false;
        }
        if (obj.constructor && !Object.prototype.hasOwnProperty.call(obj.constructor.prototype, "isPrototypeOf")) {
            return false;
        }
        return true;
    },
    isEmptyObject: function (obj: any): boolean {
        for (const name in obj) {
            return false;
        }
        return true;
    },
    type: function (obj: any): string {
        if (obj == null) {
            return obj + "";
        }
        return typeof obj === "object" || typeof obj === "function"
            ? Object.prototype.toString.call(obj).slice(8, -1).toLowerCase()
            : typeof obj;
    },
    globalEval: function (code: string): void {
        const script: HTMLScriptElement = document.createElement("script");
        script.text = code;
        document.head.appendChild(script).parentNode!.removeChild(script);
    },
    camelCase: function (str: string): string {
        return str.replace(/^-ms-/, "ms-").replace(/-([\da-z])/gi, function (all, letter) {
            return letter.toUpperCase();
        });
    },
    nodeName: function (elem: Node, name: string): boolean {
        return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();
    },
    each: function (obj: any, callback: any): any {
        let length, i = 0;

        if (Array.isArray(obj)) {
            length = obj.length;
            for (; i < length; i++) {
                if (callback.call(obj[i], i, obj[i]) === false) {
                    break;
                }
            }
        } else {
            for (i in obj) {
                if (callback.call(obj[i], i, obj[i]) === false) {
                    break;
                }
            }
        }

        return obj;
    },
    trim: function (text: string): string {
        return text == null ? "" : (text + "").replace(/^\s+|\s+$/g, "");
    },
    makeArray: function (arr: any, results: any): any {
        const ret = results || [];

        if (arr != null) {
            if (Array.isArray(Object(arr))) {
                jQuery.merge(ret, typeof arr === "string" ? [arr] : arr);
            } else {
                ret.push(arr);
            }
        }

        return ret;
    },
    inArray: function (elem: any, arr: any, i: any): any {
        return arr == null ? -1 : arr.indexOf(elem, i);
    },
    merge: function (first: any, second: any): any {
        let len = +second.length,
            j = 0,
            i = first.length;

        for (; j < len; j++) {
            first[i++] = second[j];
        }

        first.length = i;

        return first;
    },
    grep: function (elems: any, callback: any, invert: any): any {
        const callbackInverse: any,
            matches: any = [],
            i = 0,
            length = elems.length,
            callbackExpect = !invert;

        for (; i < length; i++) {
            callbackInverse = !callback(elems[i], i);
            if (callbackInverse !== callbackExpect) {
                matches.push(elems[i]);
            }
        }

        return matches;
    },
    map: function (elems: any, callback: any, arg: any): any {
        let length, value,
            i = 0,
            ret = [];

        if (Array.isArray(elems)) {
            length = elems.length;
            for (; i < length; i++) {
                value = callback(elems[i], i, arg);

                if (value != null) {
                    ret.push(value);
                }
            }
        } else {
            for (i in elems) {
                value = callback(elems[i], i, arg);

                if (value != null) {
                    ret.push(value);
                }
            }
        }

        return [].concat.apply([], ret);
    },
    guid: 1,
    proxy: function (fn: Function, context: any): Function {
        let tmp: any, args: any, proxy: any;

        if (typeof context === "string") {
            tmp = fn[context];
            context = fn;
            fn = tmp;
        }

        if (!jQuery.isFunction(fn)) {
            return undefined;
        }

        args = [].slice.call(arguments, 2);
        proxy = function (): any {
            return fn.apply(context || this, args.concat([].slice.call(arguments)));
        };

        proxy.guid = fn.guid = fn.guid || jQuery.guid++;

        return proxy;
    },
    now: Date.now,
    support: {}
});

jQuery.fn = rootjQuery;

jQuery.extend = jQuery.fn.extend = function (): any {
    let options: any, name: any, src: any, copy: any, copyIsArray: any, clone: any,
        target = arguments[0] || {},
        i = 1,
        length = arguments.length,
        deep = false;

    if (typeof target === "boolean") {
        deep = target;
        target = arguments[i] || {};
        i++;
    }

    if (typeof target !== "object" && !jQuery.isFunction(target)) {
        target = {};
    }

    if (i === length) {
        target = this;
        i--;
    }

    for (; i < length; i++) {
        if ((options = arguments[i]) != null) {
            for (name in options) {
                src = target[name];
                copy = options[name];

                if (target === copy) {
                    continue;
                }

                if (deep && copy && (jQuery.isPlainObject(copy) || (copyIsArray = Array.isArray
